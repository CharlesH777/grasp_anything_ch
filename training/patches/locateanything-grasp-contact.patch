diff --git a/Embodied/eaglevl/dist_utils.py b/Embodied/eaglevl/dist_utils.py
index 1a33d80..e4af5e2 100644
--- a/Embodied/eaglevl/dist_utils.py
+++ b/Embodied/eaglevl/dist_utils.py
@@ -3,8 +3,6 @@ import socket
 import subprocess
 from datetime import timedelta
 
-import deepspeed
-
 import torch
 import torch.multiprocessing as mp
 from torch import distributed as dist
@@ -66,6 +64,13 @@ def _init_dist_mpi(backend, **kwargs):
 
 
 def _init_dist_slurm(backend, port=None):
+    try:
+        import deepspeed
+    except ImportError as error:
+        raise RuntimeError(
+            "DeepSpeed is required when launcher='slurm'"
+        ) from error
+
     print("MASTER_PORT:",os.environ.get("MASTER_PORT", None))
     print("MASTER_ADDR:",os.environ.get("MASTER_ADDR", None))
     """Initialize slurm distributed training environment.
diff --git a/Embodied/eaglevl/model/locany/mask_sdpa_utils.py b/Embodied/eaglevl/model/locany/mask_sdpa_utils.py
index 8b216b5..1b39e16 100644
--- a/Embodied/eaglevl/model/locany/mask_sdpa_utils.py
+++ b/Embodied/eaglevl/model/locany/mask_sdpa_utils.py
@@ -190,6 +190,9 @@ def create_block_diff_mask_by_pe_4d(
     block_prefix = (~x0_flag_q & x0_flag_kv) & (kv_idx < prefix_len)
 
     final_mask = (block_causal | block_mutual | block_prefix)
+    if not final_mask.any(dim=-1).all():
+        bad_rows = (~final_mask.any(dim=-1)).nonzero(as_tuple=False).flatten()
+        raise ValueError(f"MTP attention mask contains fully masked rows: {bad_rows[:8].tolist()}")
     customized_mask = torch.full_like(final_mask, float('-inf'), dtype=torch.bfloat16)
     customized_mask.masked_fill_(final_mask, 0.0)
     
@@ -375,6 +378,9 @@ def create_mtp_packing_mask_4d(
     )
     
     final_mask = same_sample & (block_causal | block_mutual | block_prefix)
+    if not final_mask.any(dim=-1).all():
+        bad_rows = (~final_mask.any(dim=-1)).nonzero(as_tuple=False).flatten()
+        raise ValueError(f"packed MTP attention mask contains fully masked rows: {bad_rows[:8].tolist()}")
     
     attention_mask = torch.full((seq_len, seq_len), float('-inf'), dtype=torch.bfloat16, device=device)
     attention_mask.masked_fill_(final_mask, 0.0)
diff --git a/Embodied/eaglevl/model/locany/modeling_locateanything.py b/Embodied/eaglevl/model/locany/modeling_locateanything.py
index 765f0cd..97404fb 100644
--- a/Embodied/eaglevl/model/locany/modeling_locateanything.py
+++ b/Embodied/eaglevl/model/locany/modeling_locateanything.py
@@ -27,7 +27,15 @@ from transformers.utils import ModelOutput, logging
 from .configuration_locateanything import LocateAnythingConfig
 from transformers.utils import add_start_docstrings, add_start_docstrings_to_model_forward, logging, replace_return_docstrings
 from eaglevl.sp_utils import  (get_pg_manager, ring_split_for_sequence_parallel)
-from eaglevl.train.liger_loss_weight_ops import LigerFusedLinearCrossEntropyLoss
+from eaglevl.train.grasp_contact import (
+    combine_base_and_pair_ce,
+    compute_contact_auxiliary_losses,
+    compute_coordinate_token_metrics,
+    compute_task_adapter_cross_entropy,
+)
+from eaglevl.utils.locany.grasp_adapter_utils import (
+    apply_grasp_task_output_delta,
+)
 
 
 logger = logging.get_logger(__name__)
@@ -81,6 +89,7 @@ class LocateAnythingPreTrainedModel(PreTrainedModel):
 IGNORE_INDEX = -100
 class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, GenerationMixin):
     config_class = LocateAnythingConfig
+    accepts_loss_kwargs = True
     def __init__(self, config: LocateAnythingConfig, vision_model=None, language_model=None):
         super().__init__(config)
 
@@ -92,7 +101,6 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
             self.vision_model = vision_model
         else:
             if config.vision_config.model_type == 'moonvit':
-                config.vision_config._attn_implementation = 'flash_attention_2'
                 self.vision_model = MoonVitPretrainedModel(config.vision_config)
             else:
                 raise ValueError(f'Unsupported vision model type: {config.vision_config.model_type}. Only moonvit is supported.')
@@ -114,6 +122,25 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
             else:
                 raise ValueError(f'Unsupported language model architecture: {config.text_config.architectures[0]}. Only Qwen2ForCausalLM and Qwen3ForCausalLM are supported.')
 
+        task_token_ids = list(getattr(config, 'grasp_task_token_ids', []))
+        self.register_buffer(
+            '_grasp_task_token_ids',
+            torch.tensor(task_token_ids, dtype=torch.long),
+            persistent=False,
+        )
+        self.grasp_task_embedding_delta = (
+            nn.Parameter(torch.zeros(len(task_token_ids), config.text_config.hidden_size))
+            if task_token_ids
+            else None
+        )
+        self.grasp_task_output_delta = (
+            nn.Parameter(torch.zeros(len(task_token_ids), config.text_config.hidden_size))
+            if task_token_ids
+            else None
+        )
+        self._grasp_task_embedding_hook = None
+        self._grasp_task_output_hook = None
+
         vit_hidden_size = config.vision_config.hidden_size
         llm_hidden_size = config.text_config.hidden_size
 
@@ -133,6 +160,7 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
         self.use_llm_lora = config.use_llm_lora 
         if config.use_llm_lora:
             self.wrap_llm_lora(r=config.use_llm_lora, lora_alpha=2 * config.use_llm_lora)
+        self.register_grasp_task_embedding_hook()
 
         # Set _no_split_modules dynamically based on the actual LLM architecture
         arch = config.text_config.architectures[0] if hasattr(config.text_config, 'architectures') and config.text_config.architectures else 'Qwen2ForCausalLM'
@@ -141,7 +169,48 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
         else:
             self._no_split_modules = ["Qwen2DecoderLayer"]
 
-        
+    def register_grasp_task_embedding_hook(self):
+        """Inject compact trainable input/output deltas for grasp tokens."""
+        if self._grasp_task_embedding_hook is not None:
+            self._grasp_task_embedding_hook.remove()
+            self._grasp_task_embedding_hook = None
+        if self._grasp_task_output_hook is not None:
+            self._grasp_task_output_hook.remove()
+            self._grasp_task_output_hook = None
+        if self.grasp_task_embedding_delta is None:
+            return 0
+
+        def add_task_delta(_module, inputs, output):
+            token_ids = inputs[0]
+            result = output
+            delta = self.grasp_task_embedding_delta.to(dtype=output.dtype)
+            for row, token_id in enumerate(self._grasp_task_token_ids):
+                token_mask = (token_ids == token_id).unsqueeze(-1)
+                result = torch.where(token_mask, result + delta[row], result)
+            return result
+
+        embedding = self.language_model.get_input_embeddings()
+        self._grasp_task_embedding_hook = embedding.register_forward_hook(
+            add_task_delta
+        )
+
+        def add_task_logits(_module, inputs, output):
+            return apply_grasp_task_output_delta(
+                inputs[0],
+                output,
+                self._grasp_task_token_ids,
+                self.grasp_task_output_delta,
+            )
+
+        lm_head = self.language_model.get_output_embeddings()
+        self._grasp_task_output_hook = lm_head.register_forward_hook(
+            add_task_logits
+        )
+        return int(
+            self.grasp_task_embedding_delta.numel()
+            + self.grasp_task_output_delta.numel()
+        )
+
     def wrap_backbone_lora(self, r=128, lora_alpha=256, lora_dropout=0.05):
         lora_config = LoraConfig(
             r=r,
@@ -187,6 +256,20 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
             output_attentions: Optional[bool] = None,
             output_hidden_states: Optional[bool] = None,
             sub_sample_lengths: Optional[List[torch.Tensor]] = None,
+            contact_mtp_coord_mask: Optional[torch.Tensor] = None,
+            contact_candidates: Optional[torch.Tensor] = None,
+            contact_candidate_mask: Optional[torch.Tensor] = None,
+            contact_positive_mask: Optional[torch.Tensor] = None,
+            contact_image_size: Optional[torch.Tensor] = None,
+            candidate_collision_2d: Optional[torch.Tensor] = None,
+            candidate_outside_2d: Optional[torch.Tensor] = None,
+            collision_valid: Optional[torch.Tensor] = None,
+            contact_task_code: Optional[torch.Tensor] = None,
+            global_ce_tokens_in_window: Optional[torch.Tensor] = None,
+            global_contact_count_in_window: Optional[torch.Tensor] = None,
+            global_collision_valid_count_in_window: Optional[torch.Tensor] = None,
+            geometry_loss_scale: Optional[torch.Tensor] = None,
+            num_items_in_batch: Optional[torch.Tensor] = None,
             return_dict: Optional[bool] = None,
     ) -> Union[Tuple, CausalLMOutputWithPast]:
         RING_ZIGZAG = False
@@ -195,10 +278,24 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
             sub_sample_lengths = self.get_sub_sample_lengths(input_ids)
 
         input_embeds = self.language_model.get_input_embeddings()(input_ids)
+        if not torch.isfinite(input_embeds).all():
+            raise FloatingPointError(
+                "Non-finite token embeddings before visual feature insertion: "
+                f"nan={int(torch.isnan(input_embeds).sum().item())}, "
+                f"inf={int(torch.isinf(input_embeds).sum().item())}"
+            )
 
         has_images = image_flags is not None and image_flags.sum() > 0
         
         vit_embeds = self.extract_feature(pixel_values, image_grid_hws)
+        for feature_index, feature in enumerate(vit_embeds):
+            if not torch.isfinite(feature).all():
+                raise FloatingPointError(
+                    "Non-finite MoonViT output before connector: "
+                    f"feature={feature_index}, "
+                    f"nan={int(torch.isnan(feature).sum().item())}, "
+                    f"inf={int(torch.isinf(feature).sum().item())}"
+                )
             
         B, N, C = input_embeds.shape
         # LoRA's input-gradient hook can make embedding outputs leaf tensors.
@@ -220,6 +317,12 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
             vit_embeds = torch.cat(vit_embeds, dim=0)
 
             vit_embeds = self.mlp1(vit_embeds)
+            if not torch.isfinite(vit_embeds).all():
+                raise FloatingPointError(
+                    "Non-finite visual connector output: "
+                    f"nan={int(torch.isnan(vit_embeds).sum().item())}, "
+                    f"inf={int(torch.isinf(vit_embeds).sum().item())}"
+                )
             input_ids = input_ids.reshape(B * N)
             selected = (input_ids == self.image_token_index)
             n_token = int(selected.sum().item())
@@ -228,13 +331,12 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
                 input_embeds[selected] = input_embeds[selected] * 0.0 + vit_embeds
                 ignore_flag = False
             else:
-                print(f'warning: image token/feature mismatch, input_embeds[selected].shape={input_embeds[selected].shape}, '
-                      f'vit_embeds.shape={vit_embeds.shape}')
-                n_assign = min(n_token, n_embed)
-                if n_assign > 0:
-                    selected_indices = selected.nonzero(as_tuple=False).squeeze(1)[:n_assign]
-                    input_embeds[selected_indices] = input_embeds[selected_indices] * 0.0 + vit_embeds[:n_assign]
-                ignore_flag = True
+                raise ValueError(
+                    "image token/feature mismatch: "
+                    f"tokens={n_token}, visual_embeddings={n_embed}, "
+                    f"selected_shape={tuple(input_embeds[selected].shape)}, "
+                    f"visual_shape={tuple(vit_embeds.shape)}"
+                )
         else:
             ignore_flag = False
             vit_embeds = torch.cat(vit_embeds, dim=0)     
@@ -274,14 +376,31 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
             sub_sample_lengths=ssl_tensor,  # Pass sub_sample_lengths for stream packing
             )  
         
-        # not every token needs to be computed by lm_head, we only compute the tokens that have valid labels
+        # Only labeled tokens are sent through the fused LM head for the base CE.
         hidden_states = outputs.last_hidden_state
+        if not torch.isfinite(hidden_states).all():
+            nan_rows = torch.isnan(hidden_states).any(dim=-1)
+            raise FloatingPointError(
+                "Non-finite LLM hidden states: "
+                f"nan={int(torch.isnan(hidden_states).sum().item())}, "
+                f"inf={int(torch.isinf(hidden_states).sum().item())}, "
+                f"nan_tokens={int(nan_rows.sum().item())}, "
+                f"total_tokens={hidden_states.shape[-2]}, "
+                f"position_min={int(position_ids.min().item()) if position_ids is not None else None}, "
+                f"position_max={int(position_ids.max().item()) if position_ids is not None else None}, "
+                f"attention_shape={tuple(attention_mask.shape) if isinstance(attention_mask, torch.Tensor) else type(attention_mask).__name__}"
+            )
         lm_head_weight = self.language_model.lm_head.weight
         
         hidden_dim = hidden_states.shape[-1]
 
         shift_hidden_states = hidden_states[..., :-1, :].contiguous()
         shift_labels = labels[..., 1:].contiguous()
+        if 'ssl' in locals() and ssl is not None and len(ssl) > 1:
+            sample_boundaries = torch.cumsum(ssl.to(labels.device), dim=0)[:-1]
+            # A packed sample's first label must never be predicted by the
+            # previous sample, even when malformed data made it non-ignore.
+            shift_labels[..., sample_boundaries - 1] = IGNORE_INDEX
 
         shift_hidden_states = shift_hidden_states.view(-1, hidden_dim)
         shift_labels = shift_labels.view(-1)
@@ -293,14 +412,165 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
                 f"sub_sample_lengths={ssl.tolist() if 'ssl' in locals() and isinstance(ssl, torch.Tensor) else ssl if 'ssl' in locals() else None}"
             )
 
-        # Process loss_weight: shift it like labels and flatten
-        shift_loss_weight = None
         if loss_weight is not None:
-            shift_loss_weight = loss_weight[..., 1:].contiguous()
-            shift_loss_weight = shift_loss_weight.view(-1)
+            raise ValueError(
+                "loss_weight is not supported with global token normalization; "
+                "control task mixture with sampling_weight"
+            )
+
+        contact_enabled = bool(
+            getattr(self.config, 'contact_loss_enabled', False)
+        )
+        shift_coord_mask = None
+        if contact_mtp_coord_mask is not None:
+            if contact_mtp_coord_mask.shape != labels.shape:
+                raise ValueError(
+                    "contact coordinate mask must match labels before causal shift: "
+                    f"mask={tuple(contact_mtp_coord_mask.shape)}, "
+                    f"labels={tuple(labels.shape)}"
+                )
+            shift_coord_mask = contact_mtp_coord_mask[..., 1:].bool().reshape(-1)
+        elif contact_enabled:
+            raise ValueError(
+                "contact_mtp_coord_mask is required when contact loss is enabled"
+            )
+
+        base_shift_labels = shift_labels
+        if contact_enabled:
+            if shift_coord_mask is None:
+                raise ValueError(
+                    "contact_mtp_coord_mask is required when contact loss is enabled"
+                )
+            base_shift_labels = shift_labels.masked_fill(
+                shift_coord_mask, IGNORE_INDEX
+            )
+
+        valid_label_mask = base_shift_labels.ne(IGNORE_INDEX)
+        base_ce_token_count = int(valid_label_mask.sum().item())
+        if base_ce_token_count == 0:
+            raise ValueError(
+                "No base CE labels remain after contact-coordinate masking"
+            )
+        base_loss_sum = compute_task_adapter_cross_entropy(
+            self.language_model.get_output_embeddings(),
+            shift_hidden_states[valid_label_mask],
+            base_shift_labels[valid_label_mask],
+        )
+        ce_denominator = global_ce_tokens_in_window
+        if ce_denominator is None:
+            ce_denominator = num_items_in_batch
+        if ce_denominator is None:
+            ce_denominator = base_loss_sum.new_tensor(float(valid_shift_labels))
+        ce_denominator = torch.as_tensor(
+            ce_denominator, device=base_loss_sum.device, dtype=torch.float32
+        ).clamp_min(1.0)
+        loss_base = base_loss_sum / ce_denominator
+        loss = loss_base
+        loss_pair_mean = loss.new_zeros(())
+        loss_pair_contribution = loss.new_zeros(())
+
+        coordinate_ce_sum = loss.new_zeros(())
+        coordinate_top1_correct = loss.new_zeros((), dtype=torch.long)
+        coordinate_token_count = loss.new_zeros((), dtype=torch.long)
+        if shift_coord_mask is not None:
+            metric_mask = shift_coord_mask & shift_labels.ne(IGNORE_INDEX)
+            if metric_mask.any():
+                (
+                    coordinate_ce_sum,
+                    coordinate_top1_correct,
+                    coordinate_token_count,
+                ) = compute_coordinate_token_metrics(
+                    self.language_model.get_output_embeddings(),
+                    shift_hidden_states[metric_mask],
+                    shift_labels[metric_mask],
+                )
+
+        contact_output = None
+        if contact_enabled:
+            required_contact_inputs = {
+                'contact_mtp_coord_mask': contact_mtp_coord_mask,
+                'contact_candidates': contact_candidates,
+                'contact_candidate_mask': contact_candidate_mask,
+                'contact_positive_mask': contact_positive_mask,
+                'contact_image_size': contact_image_size,
+            }
+            missing = [
+                name for name, value in required_contact_inputs.items()
+                if value is None
+            ]
+            if missing:
+                raise ValueError(
+                    f"contact loss is enabled but batch is missing: {missing}"
+                )
+            contact_output = compute_contact_auxiliary_losses(
+                hidden_states=hidden_states,
+                lm_head_weight=lm_head_weight,
+                lm_head=self.language_model.get_output_embeddings(),
+                contact_mtp_coord_mask=contact_mtp_coord_mask,
+                contact_candidates=contact_candidates,
+                contact_candidate_mask=contact_candidate_mask,
+                contact_positive_mask=contact_positive_mask,
+                contact_image_size=contact_image_size,
+                coord_start_token_id=int(self.config.coord_start_token_id),
+                coord_end_token_id=int(self.config.coord_end_token_id),
+                candidate_collision_2d=candidate_collision_2d,
+                candidate_outside_2d=candidate_outside_2d,
+                collision_valid=collision_valid,
+                collision_threshold=float(
+                    getattr(self.config, 'contact_collision_threshold', 0.0)
+                ),
+                outside_threshold=float(
+                    getattr(self.config, 'contact_outside_threshold', 0.0)
+                ),
+                coord_mass_threshold=float(
+                    getattr(self.config, 'contact_coord_mass_threshold', 0.35)
+                ),
+                coord_entropy_threshold=float(
+                    getattr(self.config, 'contact_coord_entropy_threshold', 0.85)
+                ),
+                minimum_width_diagonal=float(
+                    getattr(self.config, 'contact_minimum_width_diagonal', 1e-4)
+                ),
+            )
+            contact_denominator = global_contact_count_in_window
+            if contact_denominator is None:
+                contact_denominator = contact_output.contact_count
+            contact_denominator = torch.as_tensor(
+                contact_denominator,
+                device=loss.device,
+                dtype=torch.float32,
+            ).clamp_min(1.0)
+            pair_weight = float(
+                getattr(self.config, 'contact_pair_weight', 1.0)
+            )
+            loss_pair_mean = contact_output.pair_sum / contact_denominator
+            loss_ce = combine_base_and_pair_ce(
+                base_loss_sum,
+                contact_output.pair_sum,
+                ce_denominator,
+                pair_weight,
+            )
+            loss_pair_contribution = loss_ce - loss_base
+            geometry_scale = 0.0 if geometry_loss_scale is None else geometry_loss_scale
+            geometry_scale = torch.as_tensor(
+                geometry_scale, device=loss.device, dtype=torch.float32
+            ).clamp(0.0, 1.0)
+            center_loss = contact_output.center_sum / contact_denominator
+            angle_loss = contact_output.angle_sum / contact_denominator
+            width_loss = contact_output.width_sum / contact_denominator
+            loss = (
+                loss_ce
+                + geometry_scale
+                * (
+                    float(getattr(self.config, 'contact_center_weight', 0.0))
+                    * center_loss
+                    + float(getattr(self.config, 'contact_angle_weight', 0.0))
+                    * angle_loss
+                    + float(getattr(self.config, 'contact_width_weight', 0.0))
+                    * width_loss
+                )
+            )
 
-        liger_loss_fn = LigerFusedLinearCrossEntropyLoss(ignore_index=IGNORE_INDEX, reduction='mean')
-        loss = liger_loss_fn(lm_head_weight, shift_hidden_states, shift_labels)
         if not torch.isfinite(loss):
             raise FloatingPointError(
                 f"Non-finite loss detected before backward: loss={loss.detach().float().item()}, "
@@ -310,8 +580,29 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
             )
         logits = None
 
-        if ignore_flag:
-            loss = loss * 0.0
+        self._last_loss_breakdown = {
+            'loss_total': loss.detach(),
+            'loss_base': loss_base.detach(),
+            'base_loss_sum': base_loss_sum.detach(),
+            'valid_shifted_tokens': loss.new_tensor(valid_shift_labels).detach(),
+            'base_ce_tokens': loss.new_tensor(base_ce_token_count).detach(),
+            'loss_pair_mean': loss_pair_mean.detach(),
+            'loss_pair_contribution': loss_pair_contribution.detach(),
+            'coordinate_ce_sum': coordinate_ce_sum,
+            'coordinate_top1_correct': coordinate_top1_correct,
+            'coordinate_token_count': coordinate_token_count,
+        }
+        if contact_output is not None:
+            self._last_loss_breakdown.update({
+                'loss_pair_sum': contact_output.pair_sum.detach(),
+                'loss_center_sum': contact_output.center_sum.detach(),
+                'loss_angle_sum': contact_output.angle_sum.detach(),
+                'loss_width_sum': contact_output.width_sum.detach(),
+                'contact_count': contact_output.contact_count.detach(),
+                'geometry_active_count': contact_output.geometry_active_count.detach(),
+                'coord_mass_min': contact_output.coord_mass_min.detach(),
+                'coord_entropy_max': contact_output.coord_entropy_max.detach(),
+            })
         
         if not return_dict:
             output = (logits,) + outputs[1:]
diff --git a/Embodied/eaglevl/model/moon_vit/modeling_vit.py b/Embodied/eaglevl/model/moon_vit/modeling_vit.py
index f93a43d..2023bf7 100644
--- a/Embodied/eaglevl/model/moon_vit/modeling_vit.py
+++ b/Embodied/eaglevl/model/moon_vit/modeling_vit.py
@@ -119,23 +119,25 @@ def sdpa_attention(
         q, k, v: tensor of shape (batch_size, seqlen, num_heads, head_dim),
             or (tot_seqlens, num_heads, head_dim) if packing.
     """
-    seq_length = q.shape[0]
-    attention_mask = torch.zeros(
-        [1, seq_length, seq_length], device=q.device, dtype=torch.bool
-    )
-    for i in range(1, len(q_cu_seqlens)):
-        attention_mask[
-            ...,
-            q_cu_seqlens[i - 1] : q_cu_seqlens[i],
-            q_cu_seqlens[i - 1] : q_cu_seqlens[i],
-        ] = True
-    q = q.transpose(0, 1)
-    k = k.transpose(0, 1)
-    v = v.transpose(0, 1)
-    attn_output = F.scaled_dot_product_attention(q, k, v, attention_mask, dropout_p=0.0)
-    attn_output = attn_output.transpose(0, 1)
-    attn_output = attn_output.reshape(seq_length, -1)
-    return attn_output
+    if q_cu_seqlens is None or k_cu_seqlens is None:
+        q_cu_seqlens = torch.tensor([0, q.shape[0]], device=q.device)
+        k_cu_seqlens = torch.tensor([0, k.shape[0]], device=k.device)
+    if len(q_cu_seqlens) != len(k_cu_seqlens):
+        raise ValueError("q and k packed sequence counts must match")
+
+    outputs = []
+    for index in range(1, len(q_cu_seqlens)):
+        q_start, q_end = q_cu_seqlens[index - 1], q_cu_seqlens[index]
+        k_start, k_end = k_cu_seqlens[index - 1], k_cu_seqlens[index]
+        q_segment = q[q_start:q_end].transpose(0, 1).unsqueeze(0)
+        k_segment = k[k_start:k_end].transpose(0, 1).unsqueeze(0)
+        v_segment = v[k_start:k_end].transpose(0, 1).unsqueeze(0)
+        output = F.scaled_dot_product_attention(
+            q_segment, k_segment, v_segment, dropout_p=0.0, is_causal=False
+        )
+        outputs.append(output.squeeze(0).transpose(0, 1))
+
+    return torch.cat(outputs, dim=0).reshape(q.shape[0], -1)
 
 
 def eager_attention(
diff --git a/Embodied/eaglevl/train/arguments.py b/Embodied/eaglevl/train/arguments.py
index bd8fa33..1ad2f11 100644
--- a/Embodied/eaglevl/train/arguments.py
+++ b/Embodied/eaglevl/train/arguments.py
@@ -116,6 +116,54 @@ class ModelArguments:
         default=3,
         metadata={'help': 'expected mask repeat time.'},
     )
+    contact_loss_enabled: bool = field(
+        default=False,
+        metadata={'help': 'Enable joint two-contact-point auxiliary losses.'},
+    )
+    contact_pair_weight: float = field(
+        default=1.0,
+        metadata={'help': 'Weight for exchange-invariant contact pair CE.'},
+    )
+    contact_center_weight: float = field(
+        default=0.0,
+        metadata={'help': 'Final image-space grasp center loss weight.'},
+    )
+    contact_angle_weight: float = field(
+        default=0.0,
+        metadata={'help': 'Final pi-periodic grasp angle loss weight.'},
+    )
+    contact_width_weight: float = field(
+        default=0.0,
+        metadata={'help': 'Final diagonal-normalized opening width loss weight.'},
+    )
+    contact_geometry_start_blocks: int = field(
+        default=0,
+        metadata={'help': 'Global contact blocks seen before geometry loss starts.'},
+    )
+    contact_geometry_ramp_blocks: int = field(
+        default=1,
+        metadata={'help': 'Global contact blocks used to ramp geometry loss to full weight.'},
+    )
+    contact_coord_mass_threshold: float = field(
+        default=0.35,
+        metadata={'help': 'Minimum full-vocabulary coordinate mass for geometry loss.'},
+    )
+    contact_coord_entropy_threshold: float = field(
+        default=0.85,
+        metadata={'help': 'Maximum normalized conditional coordinate entropy.'},
+    )
+    contact_minimum_width_diagonal: float = field(
+        default=1e-4,
+        metadata={'help': 'Minimum predicted opening width divided by image diagonal.'},
+    )
+    contact_collision_threshold: float = field(
+        default=0.0,
+        metadata={'help': 'Reliable candidates above this 2D collision ratio are masked.'},
+    )
+    contact_outside_threshold: float = field(
+        default=0.0,
+        metadata={'help': 'Candidates above this 2D outside-image ratio are masked.'},
+    )
 
 
 @dataclass
@@ -195,4 +243,13 @@ class DataTrainingArguments:
     auto_thinking_handler: bool = field(
         default=False,
         metadata={'help': 'Set to True to auto handle thinking mode format. Default is False.'}
-    )
\ No newline at end of file
+    )
+    contact_max_candidates: int = field(
+        default=1,
+        metadata={
+            'help': (
+                'Fixed candidate slots for every task. Keep at 1 through contact '
+                'training phases 1-4 and increase only for multi-GT training.'
+            )
+        },
+    )
diff --git a/Embodied/eaglevl/train/locany_finetune_magi_stream.py b/Embodied/eaglevl/train/locany_finetune_magi_stream.py
index 077c5b8..eade3a6 100644
--- a/Embodied/eaglevl/train/locany_finetune_magi_stream.py
+++ b/Embodied/eaglevl/train/locany_finetune_magi_stream.py
@@ -13,6 +13,7 @@ Key Features:
 import os
 import os.path as osp
 import copy
+import hashlib
 import logging
 import random
 import sys
@@ -37,24 +38,12 @@ import socket
 from eaglevl.dist_utils import init_dist
 
 import packaging.version as version
-from eaglevl.model.moon_vit.modeling_vit import MoonVitPretrainedModel
-from eaglevl.patch import (
-    replace_liger_fused_ops,
-    replace_train_dataloader,
-    replace_train_sampler
-)
-from eaglevl.model.locany.modeling_locateanything import LocateAnythingForConditionalGeneration
-from eaglevl.model.locany.configuration_locateanything import LocateAnythingConfig
-from eaglevl.utils.locany.processing_locateanything import LocateAnythingProcessor
-from eaglevl.utils.locany.image_processing_locateanything import LocateAnythingImageProcessor
 from eaglevl.sp_utils import set_pg_manager, get_pg_manager
 from eaglevl.train.constants import (
     special_tokens_list, IMG_CONTEXT_TOKEN, TEXT_MASK_TOKEN,
     NULL_TOKEN, BOX_START_TOKEN, BOX_END_TOKEN,
     REF_START_TOKEN, REF_END_TOKEN, number_tokens_list
 )
-from eaglevl.train.arguments import ModelArguments, DataTrainingArguments
-from eaglevl.train.trainer_monkey_patch import replace_create_optimizer_with_various_lr
 from PIL import Image, ImageFile, PngImagePlugin
 from torch.utils.data import Dataset, IterableDataset, DataLoader
 from transformers import (AutoConfig, AutoModelForCausalLM, AutoTokenizer,
@@ -63,10 +52,6 @@ from transformers import (AutoConfig, AutoModelForCausalLM, AutoTokenizer,
 from transformers.utils.logging import (enable_default_handler,
                                         enable_explicit_format, set_verbosity)
 from transformers import TrainerCallback
-from eaglevl.train.tools import (SaveCheckpointCallback, MemoryLoggerCallback, 
-                                  MilestoneCheckpointCallback, get_last_checkpoint_guard, 
-                                  load_config, process_multimodal_sample)
-from eaglevl.train.augmentation import apply_resize_augmentation
 from dotenv import load_dotenv
 load_dotenv()
 from transformers.trainer_pt_utils import LabelSmoother
@@ -79,23 +64,31 @@ if version.parse(torch.__version__) >= version.parse("2.4.0"):
         [np.core.multiarray._reconstruct, np.ndarray, np.dtype, type(np.dtype(np.uint32))])
 
 
-# ============ Patch ============
-replace_liger_fused_ops()
+def _install_training_patches() -> None:
+    """Install accelerator/model patches only for an actual training run."""
+    from eaglevl.patch.fused_monkey_patch import replace_liger_fused_ops
+    import transformers.modeling_utils as hf_modeling_utils
 
-# Patch HF PreTrainedModel to accept "magi" as attn_implementation.
-# HF validates attn_implementation in __init__ and only allows
-# eager/sdpa/flash_attention_2/flash_attention_3. We keep "magi" as-is so
-# the custom Qwen2 stack can dispatch to the MagiAttention path explicitly.
-import transformers.modeling_utils as _hf_modeling_utils
-_orig_check_and_adjust = _hf_modeling_utils.PreTrainedModel._check_and_adjust_attn_implementation
+    replace_liger_fused_ops()
+    current = hf_modeling_utils.PreTrainedModel._check_and_adjust_attn_implementation
+    if getattr(current, "_locany_accepts_magi", False):
+        return
 
-def _patched_check_and_adjust(self, attn_implementation, is_init_check=False):
-    if attn_implementation == "magi":
-        return "magi"
-    return _orig_check_and_adjust(self, attn_implementation, is_init_check=is_init_check)
+    def patched_check_and_adjust(
+        self, attn_implementation, is_init_check=False
+    ):
+        if attn_implementation == "magi":
+            return "magi"
+        return current(
+            self,
+            attn_implementation,
+            is_init_check=is_init_check,
+        )
 
-_hf_modeling_utils.PreTrainedModel._check_and_adjust_attn_implementation = _patched_check_and_adjust
-# ========================
+    patched_check_and_adjust._locany_accepts_magi = True
+    hf_modeling_utils.PreTrainedModel._check_and_adjust_attn_implementation = (
+        patched_check_and_adjust
+    )
 
 # ============ for loading large images ============
 Image.MAX_IMAGE_PIXELS = None
@@ -130,6 +123,59 @@ def get_rank():
     return dist.get_rank()
 
 
+def export_locany_inference_files(output_dir: str) -> None:
+    """Make a saved model directory directly loadable by AutoModel."""
+    locany_utils_src = osp.join(
+        osp.dirname(osp.dirname(osp.abspath(__file__))),
+        'utils',
+        'locany',
+    )
+    skip_files = {'config.json', 'README.md', '__init__.py', '__pycache__'}
+    if not osp.isdir(locany_utils_src):
+        raise FileNotFoundError(
+            f"LocateAnything inference sources not found: {locany_utils_src}"
+        )
+    os.makedirs(output_dir, exist_ok=True)
+    for filename in os.listdir(locany_utils_src):
+        if filename in skip_files or filename.startswith('__'):
+            continue
+        src_file = osp.join(locany_utils_src, filename)
+        if osp.isfile(src_file):
+            shutil.copy2(src_file, osp.join(output_dir, filename))
+
+    config_path = osp.join(output_dir, 'config.json')
+    if not osp.isfile(config_path):
+        raise FileNotFoundError(f"Saved checkpoint has no config.json: {output_dir}")
+    with open(config_path, 'r', encoding='utf-8') as handle:
+        config_data = json.load(handle)
+    task_token_ids = config_data.get('grasp_task_token_ids')
+    if (
+        (config_data.get('grasp_start_token_id') is None
+         or config_data.get('grasp_end_token_id') is None)
+        and isinstance(task_token_ids, list)
+        and len(task_token_ids) == 2
+    ):
+        config_data['grasp_start_token_id'] = int(task_token_ids[0])
+        config_data['grasp_end_token_id'] = int(task_token_ids[1])
+    config_data['auto_map'] = {
+        "AutoConfig": "configuration_locateanything.LocateAnythingConfig",
+        "AutoModel": "modeling_locateanything.LocateAnythingForConditionalGeneration",
+        "AutoModelForCausalLM": (
+            "modeling_locateanything.LocateAnythingForConditionalGeneration"
+        ),
+        "AutoImageProcessor": (
+            "image_processing_locateanything.LocateAnythingImageProcessor"
+        ),
+        "AutoProcessor": "processing_locateanything.LocateAnythingProcessor",
+    }
+    temporary_config_path = config_path + '.tmp'
+    with open(temporary_config_path, 'w', encoding='utf-8') as handle:
+        json.dump(config_data, handle, indent=2, ensure_ascii=False)
+        handle.write('\n')
+    os.replace(temporary_config_path, config_path)
+    logger.info("Installed inference files and auto_map in %s", output_dir)
+
+
 class LazyJsonlLoader:
     """Lazy loader for JSONL files with fast index-based access."""
     
@@ -205,7 +251,10 @@ class LazySupervisedDatasetMTP(Dataset):
                  repeat_time: float = 1,
                  max_frames: int = 16,
                  target_fps: int = 2,
-                 video_total_pixels: int = 32000 * 28 * 28 * 0.9):
+                 video_total_pixels: int = 32000 * 28 * 28 * 0.9,
+                 max_contact_candidates: Optional[int] = None,
+                 contact_collision_threshold: float = 0.0,
+                 contact_outside_threshold: float = 0.0):
         super().__init__()
         self.ds_name = ds_name
         self.processor = processor
@@ -217,15 +266,34 @@ class LazySupervisedDatasetMTP(Dataset):
         self.block_size = block_size
         self.data_augment = meta.get("data_augment", False)
         self.visual_prompt = bool(meta.get("visual_prompt", False))
+        self.task_type = meta.get("task_type")
+        self.max_contact_candidates = int(
+            max_contact_candidates
+            if max_contact_candidates is not None
+            else meta.get("max_contact_candidates", 1)
+        )
+        if self.max_contact_candidates <= 0:
+            raise ValueError("max_contact_candidates must be positive")
+        self.contact_collision_threshold = float(contact_collision_threshold)
+        self.contact_outside_threshold = float(contact_outside_threshold)
+        for name, value in (
+            ("contact_collision_threshold", self.contact_collision_threshold),
+            ("contact_outside_threshold", self.contact_outside_threshold),
+        ):
+            if not 0.0 <= value <= 1.0:
+                raise ValueError(f"{name} must be in [0, 1]")
 
         ann_paths = meta["annotation"]
         if not isinstance(ann_paths, (list, tuple)):
             ann_paths = [ann_paths]
+        self.annotation_paths = [
+            os.path.abspath(os.path.expanduser(str(path))) for path in ann_paths
+        ]
         self.root = meta.get("root", "")
         
         logger.info(f"[Dataset] {self.ds_name} Indexing JSONL files...")
         start_time = time.time()
-        self.lazy_loader = LazyJsonlLoader(ann_paths)
+        self.lazy_loader = LazyJsonlLoader(self.annotation_paths)
         logger.info(f"[Dataset] {self.ds_name} Indexing done in {time.time() - start_time:.2f}s.")
         
         original_num_rows = len(self.lazy_loader)
@@ -251,12 +319,57 @@ class LazySupervisedDatasetMTP(Dataset):
     def __len__(self):
         return self._length
 
-    def get_targets_flag_with_mtp(self, input_ids: torch.Tensor) -> Dict[str, torch.Tensor]:
+    def _contact_mtp_coordinate_mask(
+        self,
+        labels: torch.Tensor,
+        original_length: int,
+        task_type: str,
+    ) -> torch.Tensor:
+        mask = torch.zeros_like(labels, dtype=torch.bool)
+        if task_type != "grasp_contact":
+            return mask
+        if self.block_size != 6:
+            raise ValueError(
+                "grasp_contact requires block_size=6 for "
+                "[<grasp>, x1, y1, x2, y2, </grasp>]"
+            )
+
+        tokenizer = self.processor.tokenizer
+        box_start_id = tokenizer.convert_tokens_to_ids("<grasp>")
+        box_end_id = tokenizer.convert_tokens_to_ids("</grasp>")
+        coord_start_id = tokenizer.convert_tokens_to_ids("<0>")
+        coord_end_id = tokenizer.convert_tokens_to_ids("<1000>")
+        if coord_end_id - coord_start_id != 1000:
+            raise ValueError(
+                "coordinate tokens must be contiguous from <0> through <1000>"
+            )
+
+        block_count = 0
+        # targets_out inserts one bridge label after the original sequence.
+        for start in range(original_length + 1, labels.numel() - 5):
+            block = labels[start : start + 6]
+            if block[0].item() != box_start_id or block[5].item() != box_end_id:
+                continue
+            if not ((block[1:5] >= coord_start_id) & (block[1:5] <= coord_end_id)).all():
+                continue
+            mask[start + 1 : start + 5] = True
+            block_count += 1
+        if block_count != 1:
+            raise ValueError(
+                "grasp_contact must produce exactly one complete six-slot PBD block; "
+                f"found {block_count}"
+            )
+        return mask
+
+    def get_targets_flag_with_mtp(
+        self, input_ids: torch.Tensor, task_type: str = "grounding"
+    ) -> Dict[str, torch.Tensor]:
         """Create MTP (Multi-Token Prediction) blocks with proper labels."""
         tokenizer = self.processor.tokenizer
         targets_flag = torch.zeros_like(input_ids)
         
         box_end_id = tokenizer.convert_tokens_to_ids("</box>")
+        grasp_end_id = tokenizer.convert_tokens_to_ids("</grasp>")
         ref_end_id = tokenizer.convert_tokens_to_ids("</ref>")
         eos_id = tokenizer.convert_tokens_to_ids("<|im_end|>")
         null_id = tokenizer.convert_tokens_to_ids("<null>")
@@ -300,10 +413,11 @@ class LazySupervisedDatasetMTP(Dataset):
         targets_np = targets.squeeze(0).cpu().numpy()
         len_input_ids = len(input_ids_np)
         
-        # ========= 分支 1：无检测序列标记（</box>、</ref>），采用随机 block 切分 =========
+        # ========= 分支 1：无结构化定位/抓取标记，采用随机 block 切分 =========
         has_box = (input_ids == box_end_id).any().item()
         has_ref = (input_ids == ref_end_id).any().item()
-        if not (has_box or has_ref):
+        has_grasp = (input_ids == grasp_end_id).any().item()
+        if not (has_box or has_ref or has_grasp):
             all_mask_input_ids = []
             all_mask_targets = []
             all_mask_positions = []
@@ -391,6 +505,9 @@ class LazySupervisedDatasetMTP(Dataset):
                 labels=targets,
                 attention_mask=input_ids.ne(tokenizer.pad_token_id),
                 position_ids=position_ids,
+                contact_mtp_coord_mask=self._contact_mtp_coordinate_mask(
+                    targets, len_input_ids, task_type
+                ),
             )
 
         # ========= 分支 2：检测序列标记存在，使用原有的 box/ref-aware 逻辑 =========
@@ -430,8 +547,11 @@ class LazySupervisedDatasetMTP(Dataset):
                         valid_len = min(valid_len, ref_indices[0] + 1)
                     
                     box_indices = np.where(candidates[:valid_len] == box_end_id)[0]
+                    grasp_indices = np.where(candidates[:valid_len] == grasp_end_id)[0]
                     if len(box_indices) > 0:
                         valid_len = min(valid_len, box_indices[0] + 1)
+                    if len(grasp_indices) > 0:
+                        valid_len = min(valid_len, grasp_indices[0] + 1)
 
                 target_block = np.full(self.block_size, null_id, dtype=input_ids_np.dtype)
                 target_block[:valid_len] = candidates[:valid_len]
@@ -476,6 +596,9 @@ class LazySupervisedDatasetMTP(Dataset):
             labels=targets,
             attention_mask=input_ids.ne(tokenizer.pad_token_id),
             position_ids=position_ids,
+            contact_mtp_coord_mask=self._contact_mtp_coordinate_mask(
+                targets, len_input_ids, task_type
+            ),
         )
 
     def _validate_image_token_alignment(self, input_ids: torch.Tensor, pixel_values, image_grid_hws) -> None:
@@ -510,7 +633,155 @@ class LazySupervisedDatasetMTP(Dataset):
                 f"grid_hws={grid_array.tolist()}"
             )
 
-    def multi_modal_get_item(self, messages: list) -> Dict[str, torch.Tensor]:
+    def _contact_fields(self, raw_sample: dict) -> Dict[str, torch.Tensor]:
+        row_task_type = raw_sample.get("task_type", self.task_type or "grounding")
+        if self.task_type is not None and row_task_type != self.task_type:
+            raise ValueError(
+                f"dataset task_type={self.task_type!r} but row has {row_task_type!r}"
+            )
+        if row_task_type not in {
+            "grounding",
+            "grasp_contact",
+            "grasp_contact_negative",
+        }:
+            raise ValueError(f"unsupported task_type: {row_task_type!r}")
+
+        candidates = torch.zeros(
+            (1, self.max_contact_candidates, 4), dtype=torch.long
+        )
+        candidate_mask = torch.zeros(
+            (1, self.max_contact_candidates), dtype=torch.bool
+        )
+        collision_scores = torch.full(
+            (1, self.max_contact_candidates), float("nan"), dtype=torch.float32
+        )
+        outside_scores = torch.full(
+            (1, self.max_contact_candidates), float("nan"), dtype=torch.float32
+        )
+        is_positive = row_task_type == "grasp_contact"
+        raw_candidates = raw_sample.get("contact_candidates", [])
+        if is_positive:
+            if not raw_candidates:
+                raise ValueError("grasp_contact row has no contact_candidates")
+            if "image_width" not in raw_sample or "image_height" not in raw_sample:
+                raise ValueError("grasp_contact row needs image_width and image_height")
+            raw_collision = raw_sample.get("candidate_collision_2d", [])
+            raw_outside = raw_sample.get("candidate_outside_2d", [])
+            for index, candidate in enumerate(
+                raw_candidates[: self.max_contact_candidates]
+            ):
+                if not isinstance(candidate, (list, tuple)) or len(candidate) != 4:
+                    raise ValueError(f"invalid contact candidate at index {index}")
+                if not all(
+                    isinstance(value, (int, float))
+                    and float(value).is_integer()
+                    for value in candidate
+                ):
+                    raise ValueError(
+                        "contact candidates must contain integer coordinate token values"
+                    )
+                values = torch.tensor(candidate, dtype=torch.long)
+                if (values < 0).any() or (values > 1000).any():
+                    raise ValueError("contact candidate values must be in [0, 1000]")
+                if torch.equal(values[:2], values[2:]):
+                    raise ValueError("contact candidate points must not coincide")
+                candidates[0, index] = values
+                candidate_mask[0, index] = True
+                if index < len(raw_collision):
+                    collision_scores[0, index] = float(raw_collision[index])
+                if index < len(raw_outside):
+                    outside_scores[0, index] = float(raw_outside[index])
+
+            assistant_text = "".join(
+                str(message.get("value", ""))
+                for message in raw_sample.get("conversations", [])
+                if message.get("from") == "gpt"
+            )
+            primary = candidates[0, 0].tolist()
+            expected_box = "<grasp>" + "".join(
+                f"<{value}>" for value in primary
+            ) + "</grasp>"
+            if assistant_text.count(expected_box) != 1:
+                raise ValueError(
+                    "assistant contact target must contain exactly one box matching "
+                    "contact_candidates[0]"
+                )
+        elif row_task_type == "grasp_contact_negative":
+            assistant_text = "".join(
+                str(message.get("value", ""))
+                for message in raw_sample.get("conversations", [])
+                if message.get("from") == "gpt"
+            )
+            if assistant_text.count("<grasp>none</grasp>") != 1:
+                raise ValueError(
+                    "grasp_contact_negative target must contain exactly one "
+                    "<grasp>none</grasp>"
+                )
+
+        width = float(raw_sample.get("image_width", 1.0))
+        height = float(raw_sample.get("image_height", 1.0))
+        if is_positive and (width <= 0 or height <= 0):
+            raise ValueError("image_width and image_height must be positive")
+        task_code = {
+            "grounding": 0,
+            "grasp_contact": 1,
+            "grasp_contact_negative": 2,
+        }[row_task_type]
+        collision_valid = bool(raw_sample.get("collision_valid", False))
+        if collision_valid and is_positive:
+            valid_scores = collision_scores[0, candidate_mask[0]]
+            if (
+                not torch.isfinite(valid_scores).all()
+                or (valid_scores < 0).any()
+                or (valid_scores > 1).any()
+            ):
+                raise ValueError(
+                    "collision_valid contact row needs one [0, 1] score per candidate"
+                )
+        else:
+            collision_valid = False
+        if is_positive:
+            valid_outside_scores = outside_scores[0, candidate_mask[0]]
+            if (
+                not torch.isfinite(valid_outside_scores).all()
+                or (valid_outside_scores < 0).any()
+                or (valid_outside_scores > 1).any()
+            ):
+                raise ValueError(
+                    "grasp_contact row needs one finite candidate_outside_2d "
+                    "score per candidate"
+                )
+
+            safe_candidates = candidate_mask.clone()
+            safe_candidates &= torch.isfinite(outside_scores) & (
+                outside_scores <= self.contact_outside_threshold
+            )
+            if collision_valid:
+                safe_candidates &= torch.isfinite(collision_scores) & (
+                    collision_scores <= self.contact_collision_threshold
+                )
+            if not safe_candidates[0, 0]:
+                raise ValueError(
+                    "assistant contact target is unsafe at training thresholds"
+                )
+            candidate_mask &= safe_candidates
+
+        return {
+            "contact_candidates": candidates,
+            "contact_candidate_mask": candidate_mask,
+            "contact_positive_mask": torch.tensor([is_positive], dtype=torch.bool),
+            "contact_image_size": torch.tensor([[width, height]], dtype=torch.float32),
+            "candidate_collision_2d": collision_scores,
+            "candidate_outside_2d": outside_scores,
+            "collision_valid": torch.tensor([collision_valid], dtype=torch.bool),
+            "contact_task_code": torch.tensor([task_code], dtype=torch.long),
+        }
+
+    def multi_modal_get_item(
+        self, messages: list, raw_sample: dict
+    ) -> Dict[str, torch.Tensor]:
+        from eaglevl.train.augmentation import apply_resize_augmentation
+
         message_text = self.processor.py_apply_chat_template(messages, tokenize=False)
         image_inputs, video_inputs = self.processor.process_vision_info(messages)
         
@@ -538,7 +809,9 @@ class LazySupervisedDatasetMTP(Dataset):
             image_flags = torch.tensor([len(inputs["image_grid_hws"])], dtype=torch.long)
             self._validate_image_token_alignment(input_ids, pixel_values, image_grid_hws)
 
-        labels_dict = self.get_targets_flag_with_mtp(input_ids)
+        task_type = raw_sample.get("task_type", self.task_type or "grounding")
+        labels_dict = self.get_targets_flag_with_mtp(input_ids, task_type=task_type)
+        contact_fields = self._contact_fields(raw_sample)
         
         return dict(
             input_ids=labels_dict["input_ids"],
@@ -548,74 +821,93 @@ class LazySupervisedDatasetMTP(Dataset):
             image_flags=image_flags,
             pixel_values=pixel_values,
             image_grid_hws=image_grid_hws,
+            contact_mtp_coord_mask=labels_dict["contact_mtp_coord_mask"],
+            **contact_fields,
         )
 
     def __getitem__(self, idx: int) -> Dict[str, torch.Tensor]:
-        retry_count = 0
-        current_idx = idx
-        
+        from eaglevl.train.tools import process_multimodal_sample
+
         seed = int(idx + 10086)
         random.seed(seed)
         np.random.seed(seed)
-        
-        while retry_count <= 10:
-            real_idx = self.active_indices[(current_idx + retry_count) % self._length]
-            try:
-                data_item = self.lazy_loader[real_idx]
-                data_item = process_multimodal_sample(
-                    data_item, self.root, self.max_frames, 
-                    self.target_fps, self.video_total_pixels,
-                    visual_prompt=self.visual_prompt,
-                )
-                return self.multi_modal_get_item(data_item)
-            except Exception as e:
-                tb = traceback.format_exc()
-                logger.warning(f"[{self.ds_name}] idx {real_idx} failed: {e}\n{tb}")
-                retry_count += 1
-        
-        raise RuntimeError(f"[{self.ds_name}] Failed after 10 retries")
-    
-    def get_sample_at_global_idx(self, global_idx: int, seed: int) -> Dict[str, torch.Tensor]:
-        """Get a sample by global index (used for resume)."""
-        ds_len = self._length
-        if ds_len == 0:
-            raise ValueError("Dataset is empty")
-        
-        epoch = global_idx // ds_len
-        pos = global_idx % ds_len
-        
-        shuffle_seed = seed + epoch * 999983
-        rng = random.Random(shuffle_seed)
-        indices = list(range(ds_len))
-        rng.shuffle(indices)
-        
-        real_idx = indices[pos]
-        return self[real_idx]
 
+        real_idx = self.active_indices[idx]
+        try:
+            raw_sample = self.lazy_loader[real_idx]
+            data_item = process_multimodal_sample(
+                copy.deepcopy(raw_sample), self.root, self.max_frames,
+                self.target_fps, self.video_total_pixels,
+                visual_prompt=self.visual_prompt,
+            )
+            return self.multi_modal_get_item(data_item, raw_sample)
+        except Exception as error:
+            tb = traceback.format_exc()
+            logger.warning(
+                f"[{self.ds_name}] idx {real_idx} failed: {error}\n{tb}"
+            )
+            raise
 
 @dataclass
 class IteratorState:
     """Iterator state."""
     seed: int
     global_idx: int
+    shard_id: int = 0
+    num_shards: int = 1
     
     def to_dict(self) -> dict:
-        return {'seed': self.seed, 'global_idx': self.global_idx}
+        return {
+            'seed': self.seed,
+            'global_idx': self.global_idx,
+            'shard_id': self.shard_id,
+            'num_shards': self.num_shards,
+        }
     
     @classmethod
     def from_dict(cls, d: dict) -> 'IteratorState':
-        return cls(seed=d['seed'], global_idx=d['global_idx'])
+        return cls(
+            seed=d['seed'],
+            global_idx=d['global_idx'],
+            shard_id=d.get('shard_id', 0),
+            num_shards=d.get('num_shards', 1),
+        )
 
 
 class DeterministicIterator:
-    """Deterministic dataset iterator."""
+    """Deterministic, mutually exclusive shard of an infinite dataset stream."""
     
-    def __init__(self, dataset: LazySupervisedDatasetMTP, seed: int, start_global_idx: int = 0):
+    def __init__(
+        self,
+        dataset: LazySupervisedDatasetMTP,
+        seed: int,
+        start_global_idx: Optional[int] = None,
+        shard_id: int = 0,
+        num_shards: int = 1,
+    ):
+        if num_shards <= 0:
+            raise ValueError(f"num_shards must be positive, got {num_shards}")
+        if not 0 <= shard_id < num_shards:
+            raise ValueError(
+                f"shard_id must be in [0, {num_shards}), got {shard_id}"
+            )
+
         self.dataset = dataset
         self.seed = seed
         self.ds_len = len(dataset)
         self.ds_name = getattr(dataset, 'ds_name', 'unknown')
-        self.global_idx = start_global_idx
+        self.shard_id = shard_id
+        self.num_shards = num_shards
+        # Permanent ownership prevents a faster packing worker from entering a
+        # new global permutation and repeating samples owned by a slower one.
+        self._owned_indices = list(range(shard_id, self.ds_len, num_shards))
+        self.global_idx = shard_id if start_global_idx is None else start_global_idx
+        if self.global_idx % self.num_shards != self.shard_id:
+            raise ValueError(
+                "start_global_idx does not belong to this shard: "
+                f"global_idx={self.global_idx}, shard_id={self.shard_id}, "
+                f"num_shards={self.num_shards}"
+            )
         
         self._cached_epoch = -1
         self._cached_indices = None
@@ -626,7 +918,7 @@ class DeterministicIterator:
         
         shuffle_seed = self.seed + epoch * 999983
         rng = random.Random(shuffle_seed)
-        indices = list(range(self.ds_len))
+        indices = list(self._owned_indices)
         rng.shuffle(indices)
         
         self._cached_epoch = epoch
@@ -637,29 +929,96 @@ class DeterministicIterator:
         return self
     
     def __next__(self) -> Tuple[dict, int]:
-        if self.ds_len == 0:
+        if not self._owned_indices:
             raise StopIteration
-        
-        current_global_idx = self.global_idx
-        epoch = current_global_idx // self.ds_len
-        pos = current_global_idx % self.ds_len
-        indices = self._get_epoch_indices(epoch)
-        
-        real_idx = indices[pos]
-        sample = self.dataset[real_idx]
-        self.global_idx += 1
-        
-        return sample, current_global_idx
+
+        last_error = None
+        for _ in range(11):
+            current_global_idx = self.global_idx
+            local_cursor = (
+                current_global_idx - self.shard_id
+            ) // self.num_shards
+            shard_length = len(self._owned_indices)
+            epoch = local_cursor // shard_length
+            pos = local_cursor % shard_length
+            indices = self._get_epoch_indices(epoch)
+            real_idx = indices[pos]
+            self.global_idx += self.num_shards
+            try:
+                sample = self.dataset[real_idx]
+                return sample, current_global_idx
+            except Exception as error:
+                last_error = error
+                logger.warning(
+                    "[%s] shard %s skipped owned index %s after read failure: %s",
+                    self.ds_name,
+                    self.shard_id,
+                    real_idx,
+                    error,
+                )
+        raise RuntimeError(
+            f"[{self.ds_name}] shard {self.shard_id} failed 11 owned samples"
+        ) from last_error
+
+    def sample_at_global_idx(self, global_idx: int) -> dict:
+        """Rebuild one buffered sample from this shard's logical cursor."""
+        if global_idx % self.num_shards != self.shard_id:
+            raise ValueError(
+                f"global_idx={global_idx} is outside shard {self.shard_id}"
+            )
+        if not self._owned_indices:
+            raise ValueError("cannot restore a sample from an empty shard")
+        local_cursor = (global_idx - self.shard_id) // self.num_shards
+        if local_cursor < 0:
+            raise ValueError(f"global_idx must be non-negative, got {global_idx}")
+        shard_length = len(self._owned_indices)
+        epoch = local_cursor // shard_length
+        pos = local_cursor % shard_length
+        return self.dataset[self._get_epoch_indices(epoch)[pos]]
     
     def peek_global_idx(self) -> int:
         return self.global_idx
     
     def state_dict(self) -> dict:
-        return IteratorState(seed=self.seed, global_idx=self.global_idx).to_dict()
+        return IteratorState(
+            seed=self.seed,
+            global_idx=self.global_idx,
+            shard_id=self.shard_id,
+            num_shards=self.num_shards,
+        ).to_dict()
     
     @classmethod
-    def from_state_dict(cls, dataset: LazySupervisedDatasetMTP, state: dict) -> 'DeterministicIterator':
-        return cls(dataset=dataset, seed=state['seed'], start_global_idx=state['global_idx'])
+    def from_state_dict(
+        cls,
+        dataset: LazySupervisedDatasetMTP,
+        state: dict,
+        *,
+        shard_id: Optional[int] = None,
+        num_shards: Optional[int] = None,
+    ) -> 'DeterministicIterator':
+        iterator_state = IteratorState.from_dict(state)
+        expected_shard_id = (
+            iterator_state.shard_id if shard_id is None else shard_id
+        )
+        expected_num_shards = (
+            iterator_state.num_shards if num_shards is None else num_shards
+        )
+        if (
+            iterator_state.shard_id != expected_shard_id
+            or iterator_state.num_shards != expected_num_shards
+        ):
+            raise ValueError(
+                "Iterator shard topology changed across resume: "
+                f"saved={iterator_state.shard_id}/{iterator_state.num_shards}, "
+                f"current={expected_shard_id}/{expected_num_shards}"
+            )
+        return cls(
+            dataset=dataset,
+            seed=iterator_state.seed,
+            start_global_idx=iterator_state.global_idx,
+            shard_id=expected_shard_id,
+            num_shards=expected_num_shards,
+        )
 
 
 @dataclass
@@ -730,6 +1089,16 @@ class StreamPackedDatasetMTP(IterableDataset):
         self.base_seed = base_seed
         self.buffer_size = buffer_size
 
+        if self.data_world_size <= 0:
+            raise ValueError(
+                f"data_world_size must be positive, got {self.data_world_size}"
+            )
+        if not 0 <= self.data_rank < self.data_world_size:
+            raise ValueError(
+                f"data_rank must be in [0, {self.data_world_size}), "
+                f"got {self.data_rank}"
+            )
+
         if dataset_weight is None:
             dataset_weight = [1] * len(datasets)
         total_weight = sum(dataset_weight)
@@ -737,6 +1106,90 @@ class StreamPackedDatasetMTP(IterableDataset):
         
         self._worker_states: Dict[str, dict] = {}
         self._resume_states: Dict[str, dict] = {}
+        self._resume_required = False
+        fingerprint_payload = []
+        for dataset, normalized_weight in zip(
+            self.datasets, self.dataset_weight
+        ):
+            processor = getattr(dataset, 'processor', None)
+            tokenizer = getattr(processor, 'tokenizer', None)
+            image_processor = getattr(processor, 'image_processor', None)
+            image_processor_config = None
+            if image_processor is not None and hasattr(image_processor, 'to_dict'):
+                image_processor_config = image_processor.to_dict()
+            annotations = []
+            for annotation_path in getattr(dataset, 'annotation_paths', []):
+                stat = os.stat(annotation_path)
+                annotations.append({
+                    'path': annotation_path,
+                    'size': stat.st_size,
+                    'mtime_ns': stat.st_mtime_ns,
+                })
+            fingerprint_payload.append({
+                'name': getattr(dataset, 'ds_name', None),
+                'length': len(dataset),
+                'root': os.path.abspath(os.path.expanduser(
+                    str(getattr(dataset, 'root', ''))
+                )),
+                'task_type': getattr(dataset, 'task_type', None),
+                'block_size': getattr(dataset, 'block_size', None),
+                'max_contact_candidates': getattr(
+                    dataset, 'max_contact_candidates', None
+                ),
+                'contact_collision_threshold': getattr(
+                    dataset, 'contact_collision_threshold', None
+                ),
+                'contact_outside_threshold': getattr(
+                    dataset, 'contact_outside_threshold', None
+                ),
+                'data_augment': getattr(dataset, 'data_augment', None),
+                'visual_prompt': getattr(dataset, 'visual_prompt', None),
+                'max_frames': getattr(dataset, 'max_frames', None),
+                'target_fps': getattr(dataset, 'target_fps', None),
+                'video_total_pixels': getattr(
+                    dataset, 'video_total_pixels', None
+                ),
+                'max_length': getattr(dataset, 'max_length', None),
+                'processor_class': (
+                    type(processor).__qualname__ if processor is not None else None
+                ),
+                'tokenizer_class': (
+                    type(tokenizer).__qualname__ if tokenizer is not None else None
+                ),
+                'tokenizer_path': getattr(tokenizer, 'tokenizer_path', None),
+                'tokenizer_vocab_size': (
+                    len(tokenizer) if tokenizer is not None else None
+                ),
+                'tokenizer_added_vocab': (
+                    tokenizer.get_added_vocab()
+                    if tokenizer is not None
+                    and hasattr(tokenizer, 'get_added_vocab')
+                    else None
+                ),
+                'image_processor_class': (
+                    type(image_processor).__qualname__
+                    if image_processor is not None
+                    else None
+                ),
+                'image_processor_config': image_processor_config,
+                'sampling_weight': normalized_weight,
+                'annotations': annotations,
+            })
+        fingerprint_payload.append({
+            'stream_config': {
+                'max_num_tokens_per_sample': self.max_num_tokens_per_sample,
+                'max_num_tokens': self.max_num_tokens,
+                'buffer_size': self.buffer_size,
+            }
+        })
+        self.data_fingerprint = hashlib.sha256(
+            json.dumps(
+                fingerprint_payload,
+                sort_keys=True,
+                separators=(',', ':'),
+                default=str,
+            ).encode('utf-8')
+        ).hexdigest()
         
         if get_rank() == 0:
             ds_info = '\n'.join([f'  {ds.ds_name}: weight={w*100:.2f}%, len={len(ds)}' 
@@ -753,18 +1206,55 @@ class StreamPackedDatasetMTP(IterableDataset):
         return {
             'worker_states': copy.deepcopy(self._worker_states),
             'base_seed': self.base_seed,
-            'version': 4,
+            'data_fingerprint': self.data_fingerprint,
+            'version': 6,
         }
     
     def load_state_dict(self, state: dict):
         version = state.get('version', 1)
-        if version < 3:
-            logger.warning(f"Loading old state version {version}, perfect resume not available.")
+        if version < 6:
+            raise ValueError(
+                "dataloader state predates dataset fingerprints/shard ownership "
+                f"(version={version}); load this checkpoint through MODEL_PATH "
+                "with a fresh optimizer and data stream instead of resuming"
+            )
+        saved_fingerprint = state.get('data_fingerprint')
+        if saved_fingerprint != self.data_fingerprint:
+            raise ValueError(
+                "dataloader dataset fingerprint mismatch: "
+                f"saved={saved_fingerprint}, current={self.data_fingerprint}"
+            )
+        if int(state.get('base_seed', self.base_seed)) != self.base_seed:
+            raise ValueError(
+                "dataloader base_seed changed across resume: "
+                f"saved={state.get('base_seed')}, current={self.base_seed}"
+            )
         
-        if 'worker_states' in state:
-            self._resume_states = copy.deepcopy(state['worker_states'])
-            if get_rank() == 0:
-                logger.info(f"Loaded resume states for {len(self._resume_states)} workers")
+        worker_states = state.get('worker_states')
+        if not isinstance(worker_states, dict) or not worker_states:
+            raise ValueError("dataloader resume state has no worker snapshots")
+        self._resume_states = copy.deepcopy(worker_states)
+        self._resume_required = True
+        if get_rank() == 0:
+            logger.info(
+                f"Loaded resume states for {len(self._resume_states)} workers"
+            )
+
+    def _worker_partition(
+        self,
+        local_worker_id: int,
+        num_workers: int,
+    ) -> Tuple[int, int]:
+        if num_workers <= 0:
+            raise ValueError(f"num_workers must be positive, got {num_workers}")
+        if not 0 <= local_worker_id < num_workers:
+            raise ValueError(
+                f"local_worker_id must be in [0, {num_workers}), "
+                f"got {local_worker_id}"
+            )
+        global_worker_id = num_workers * self.data_rank + local_worker_id
+        total_workers = num_workers * self.data_world_size
+        return global_worker_id, total_workers
 
     def _get_sample_length(self, sample: Optional[dict]) -> int:
         if sample is None:
@@ -815,7 +1305,20 @@ class StreamPackedDatasetMTP(IterableDataset):
         local_worker_id = 0 if worker_info is None else worker_info.id
         num_workers = 1 if worker_info is None else worker_info.num_workers
         
-        global_worker_id = num_workers * self.data_rank + local_worker_id
+        global_worker_id, total_workers = self._worker_partition(
+            local_worker_id, num_workers
+        )
+        undersized = [
+            (getattr(dataset, 'ds_name', str(index)), len(dataset))
+            for index, dataset in enumerate(self.datasets)
+            if len(dataset) < total_workers
+        ]
+        if undersized:
+            raise ValueError(
+                "every sampled dataset must have at least one permanently owned "
+                f"sample per rank/worker shard (total_workers={total_workers}): "
+                f"{undersized}"
+            )
         worker_key = f'worker_{global_worker_id}'
         is_main_log = (global_worker_id == 0)
         
@@ -827,11 +1330,18 @@ class StreamPackedDatasetMTP(IterableDataset):
         sample_rng = random.Random(worker_seed)
         
         iterators: List[DeterministicIterator] = []
-        iterator_seeds: List[int] = []
         for ds_idx, ds in enumerate(self.datasets):
-            iter_seed = self.base_seed + global_worker_id * 10000 + ds_idx
-            iterator_seeds.append(iter_seed)
-            iterators.append(DeterministicIterator(ds, seed=iter_seed, start_global_idx=0))
+            # All workers share an epoch permutation. The iterator's stride then
+            # partitions that common stream across every rank and worker.
+            iter_seed = self.base_seed + ds_idx
+            iterators.append(
+                DeterministicIterator(
+                    ds,
+                    seed=iter_seed,
+                    shard_id=global_worker_id,
+                    num_shards=total_workers,
+                )
+            )
         
         samples_produced = 0
         batches_produced = 0
@@ -842,6 +1352,11 @@ class StreamPackedDatasetMTP(IterableDataset):
         buffer: List[Tuple[dict, int, int]] = []
         
         # Resume handling
+        if self._resume_required and worker_key not in self._resume_states:
+            raise RuntimeError(
+                f"missing resume state for {worker_key}; refusing a partial "
+                "dataloader resume"
+            )
         if worker_key in self._resume_states:
             saved = self._resume_states.pop(worker_key)
             try:
@@ -849,10 +1364,18 @@ class StreamPackedDatasetMTP(IterableDataset):
                 
                 if is_main_log:
                     logger.info(f'[{worker_key}] Resuming...')
-                
+                if len(ws.iterator_states) != len(self.datasets):
+                    raise ValueError(
+                        "resume iterator/dataset count mismatch: "
+                        f"iterators={len(ws.iterator_states)}, "
+                        f"datasets={len(self.datasets)}"
+                    )
                 for ds_idx, iter_state in enumerate(ws.iterator_states):
                     iterators[ds_idx] = DeterministicIterator.from_state_dict(
-                        self.datasets[ds_idx], iter_state
+                        self.datasets[ds_idx],
+                        iter_state,
+                        shard_id=global_worker_id,
+                        num_shards=total_workers,
                     )
                 
                 sample_rng.setstate(ws.sample_rng_state)
@@ -865,12 +1388,17 @@ class StreamPackedDatasetMTP(IterableDataset):
                     for loc in ws.current_batch_locations:
                         ds_idx, global_idx = loc
                         try:
-                            sample = self.datasets[ds_idx].get_sample_at_global_idx(global_idx, iterator_seeds[ds_idx])
-                            if self._get_sample_length(sample) <= self.max_num_tokens_per_sample:
-                                current_batch = self._merge_samples(current_batch, sample)
-                                current_batch_locations.append(loc)
+                            sample = iterators[ds_idx].sample_at_global_idx(global_idx)
+                            if self._get_sample_length(sample) > self.max_num_tokens_per_sample:
+                                raise ValueError(
+                                    f"restored sample {loc} now exceeds token limit"
+                                )
+                            current_batch = self._merge_samples(current_batch, sample)
+                            current_batch_locations.append(loc)
                         except Exception as e:
-                            logger.warning(f'[{worker_key}] Failed to restore batch sample {loc}: {e}')
+                            raise RuntimeError(
+                                f"{worker_key} failed to restore batch sample {loc}"
+                            ) from e
 
                 if ws.buffer_locations:
                     if is_main_log:
@@ -878,11 +1406,16 @@ class StreamPackedDatasetMTP(IterableDataset):
                     for loc in ws.buffer_locations:
                         ds_idx, global_idx = loc
                         try:
-                            sample = self.datasets[ds_idx].get_sample_at_global_idx(global_idx, iterator_seeds[ds_idx])
-                            if self._get_sample_length(sample) <= self.max_num_tokens_per_sample:
-                                buffer.append((sample, ds_idx, global_idx))
+                            sample = iterators[ds_idx].sample_at_global_idx(global_idx)
+                            if self._get_sample_length(sample) > self.max_num_tokens_per_sample:
+                                raise ValueError(
+                                    f"restored sample {loc} now exceeds token limit"
+                                )
+                            buffer.append((sample, ds_idx, global_idx))
                         except Exception as e:
-                            logger.warning(f'[{worker_key}] Failed to restore buffer sample {loc}: {e}')
+                            raise RuntimeError(
+                                f"{worker_key} failed to restore buffer sample {loc}"
+                            ) from e
                 
                 if is_main_log:
                     logger.info(f'[{worker_key}] Resume complete. Buffer size: {len(buffer)}')
@@ -890,9 +1423,9 @@ class StreamPackedDatasetMTP(IterableDataset):
             except Exception as e:
                 logger.error(f'[{worker_key}] Failed to resume: {e}')
                 traceback.print_exc()
-                current_batch = None
-                current_batch_locations = []
-                buffer = []
+                raise RuntimeError(
+                    f"{worker_key} could not restore its exact data stream"
+                ) from e
 
         # Helper functions
         def build_state_snapshot() -> dict:
@@ -1000,6 +1533,8 @@ def packed_collate_fn_mtp(features: List[dict], dataset: Optional[StreamPackedDa
     sub_sample_lengths = feat['sub_sample_lengths']
     packed_len = int(sub_sample_lengths.sum().item()) if isinstance(sub_sample_lengths, torch.Tensor) else int(sum(sub_sample_lengths))
     non_ignore_labels = int(feat['labels'][1:].ne(IGNORE_TOKEN_ID).sum().item()) if feat['labels'].numel() > 1 else 0
+    contact_coord_slots = int(feat['contact_mtp_coord_mask'].sum().item())
+    contact_positive_samples = int(feat['contact_positive_mask'].sum().item())
 
     if not (input_len == label_len == pos_len == packed_len):
         raise ValueError(
@@ -1012,6 +1547,16 @@ def packed_collate_fn_mtp(features: List[dict], dataset: Optional[StreamPackedDa
             f"labels_non_ignore_after_shift={non_ignore_labels}, "
             f"sub_sample_lengths={sub_sample_lengths.tolist() if isinstance(sub_sample_lengths, torch.Tensor) else sub_sample_lengths}"
         )
+    if feat['contact_mtp_coord_mask'].numel() != label_len:
+        raise ValueError(
+            "contact_mtp_coord_mask must be label-aligned: "
+            f"mask={feat['contact_mtp_coord_mask'].numel()}, labels={label_len}"
+        )
+    if contact_coord_slots != 4 * contact_positive_samples:
+        raise ValueError(
+            "each positive contact sample must contribute exactly four MTP slots: "
+            f"slots={contact_coord_slots}, positives={contact_positive_samples}"
+        )
     
     worker_key = feat.get('_worker_key', None)
     state_snapshot = feat.get('_state_snapshot', None)
@@ -1030,6 +1575,15 @@ def packed_collate_fn_mtp(features: List[dict], dataset: Optional[StreamPackedDa
         pixel_values=feat['pixel_values'],
         image_flags=image_flags,
         sub_sample_lengths=[sub_sample_lengths],
+        contact_mtp_coord_mask=feat['contact_mtp_coord_mask'].unsqueeze(0),
+        contact_candidates=feat['contact_candidates'],
+        contact_candidate_mask=feat['contact_candidate_mask'],
+        contact_positive_mask=feat['contact_positive_mask'],
+        contact_image_size=feat['contact_image_size'],
+        candidate_collision_2d=feat['candidate_collision_2d'],
+        candidate_outside_2d=feat['candidate_outside_2d'],
+        collision_valid=feat['collision_valid'],
+        contact_task_code=feat['contact_task_code'],
     )
 
     if 'image_grid_hws' in feat:
@@ -1108,7 +1662,11 @@ class DataloaderStateCallback(TrainerCallback):
                     ws.get('samples_produced', 0) 
                     for ws in ds_state.get('worker_states', {}).values()
                 )
-                logger.info(f"Saving dataloader state: total_batches={total_batches}")
+                logger.info(
+                    "Saving dataloader state: total_batches=%s, total_samples=%s",
+                    total_batches,
+                    total_samples,
+                )
             
             torch.save(ds_state, temp_path)
             os.replace(temp_path, state_path)
@@ -1136,6 +1694,302 @@ class StreamPackingMTPTrainer(Trainer):
         self._total_samples = 0
         self._sample_log_interval = sample_log_interval
         self._start_step = None  # 记录开始的step，用于resume时正确计算平均值
+        self._seen_contact_blocks = 0
+        self._last_contact_log_step = -1
+        self._last_window_counts = None
+        self._last_geometry_scale = 0.0
+        self._coordinate_metric_window = None
+        self._loss_breakdown_window = {}
+        self._loss_breakdown_steps = set()
+
+    def _accumulate_coordinate_metrics(self, breakdown) -> None:
+        names = (
+            'coordinate_ce_sum',
+            'coordinate_top1_correct',
+            'coordinate_token_count',
+        )
+        if any(name not in breakdown for name in names):
+            return
+        values = [breakdown[name] for name in names]
+        if any(not torch.is_tensor(value) or value.numel() != 1 for value in values):
+            raise ValueError("coordinate metric breakdown values must be scalars")
+        current = torch.stack(
+            [value.detach().float().reshape(()) for value in values]
+        )
+        if self._coordinate_metric_window is None:
+            self._coordinate_metric_window = current.clone()
+        else:
+            self._coordinate_metric_window = (
+                self._coordinate_metric_window.to(current.device) + current
+            )
+
+    def _consume_coordinate_metrics(self):
+        if self._coordinate_metric_window is None:
+            return {}
+        totals = self._coordinate_metric_window
+        self._coordinate_metric_window = None
+        if is_dist_avail_and_initialized():
+            dist.all_reduce(totals, op=dist.ReduceOp.SUM)
+        count = float(totals[2].item())
+        if count <= 0.0:
+            return {
+                'contact/coordinate_ce': 0.0,
+                'contact/coordinate_top1_accuracy': 0.0,
+            }
+        return {
+            'contact/coordinate_ce': float(totals[0].item() / count),
+            'contact/coordinate_top1_accuracy': float(totals[1].item() / count),
+        }
+
+    def _accumulate_loss_breakdown(self, breakdown, optimizer_step: int) -> None:
+        coordinate_metric_parts = {
+            'coordinate_ce_sum',
+            'coordinate_top1_correct',
+            'coordinate_token_count',
+        }
+        for name, value in breakdown.items():
+            if name in coordinate_metric_parts:
+                continue
+            if not torch.is_tensor(value) or value.numel() != 1:
+                continue
+            current = value.detach().float().reshape(()).clone()
+            if name not in self._loss_breakdown_window:
+                self._loss_breakdown_window[name] = current
+            elif name == 'coord_mass_min':
+                self._loss_breakdown_window[name] = torch.minimum(
+                    self._loss_breakdown_window[name].to(current.device), current
+                )
+            elif name == 'coord_entropy_max':
+                self._loss_breakdown_window[name] = torch.maximum(
+                    self._loss_breakdown_window[name].to(current.device), current
+                )
+            else:
+                self._loss_breakdown_window[name] = (
+                    self._loss_breakdown_window[name].to(current.device) + current
+                )
+        self._loss_breakdown_steps.add(int(optimizer_step))
+
+    def _consume_loss_breakdown(self):
+        if not self._loss_breakdown_window:
+            return {}
+        optimizer_steps = max(1, len(self._loss_breakdown_steps))
+        logs = {}
+        for name, value in self._loss_breakdown_window.items():
+            reduced = value.clone()
+            reduction = self._breakdown_reduce_op(name)
+            if is_dist_avail_and_initialized():
+                dist.all_reduce(reduced, op=reduction)
+            if reduction == dist.ReduceOp.SUM:
+                reduced /= optimizer_steps
+            logs[f'contact/{name}'] = float(reduced.item())
+        self._loss_breakdown_window = {}
+        self._loss_breakdown_steps = set()
+        return logs
+
+    @staticmethod
+    def _breakdown_reduce_op(name):
+        if name == 'coord_mass_min':
+            return dist.ReduceOp.MIN
+        if name == 'coord_entropy_max':
+            return dist.ReduceOp.MAX
+        return dist.ReduceOp.SUM
+
+    @staticmethod
+    def _shifted_label_count(batch):
+        labels = batch['labels']
+        labels = labels[0] if labels.ndim == 2 else labels
+
+        def count_slice(start, end):
+            return labels[start:end].ne(IGNORE_TOKEN_ID).sum()
+
+        lengths = batch.get('sub_sample_lengths')
+        if isinstance(lengths, list):
+            lengths = lengths[0]
+        if lengths is None:
+            return count_slice(1, labels.numel())
+        offset = 0
+        count = labels.new_zeros(())
+        for raw_length in lengths:
+            length = int(raw_length.item())
+            count = count + count_slice(offset + 1, offset + length)
+            offset += length
+        if offset != labels.numel():
+            raise ValueError(
+                f"shifted-label count saw packed length {offset}, "
+                f"expected {labels.numel()}"
+            )
+        return count
+
+    def get_batch_samples(self, epoch_iterator, num_batches, device):
+        batch_samples = []
+        for _ in range(num_batches):
+            try:
+                batch_samples.append(next(epoch_iterator))
+            except StopIteration:
+                break
+        if not batch_samples:
+            return batch_samples, None
+
+        unwrapped_model = self.accelerator.unwrap_model(self.model)
+        config = unwrapped_model.config
+        local_counts = torch.stack([
+            sum(
+                (
+                    self._shifted_label_count(
+                        batch,
+                    )
+                    for batch in batch_samples
+                ),
+                start=torch.zeros((), dtype=torch.long),
+            ),
+            sum(
+                (batch['contact_positive_mask'].sum() for batch in batch_samples),
+                start=torch.zeros((), dtype=torch.long),
+            ),
+            sum(
+                (batch['collision_valid'].sum() for batch in batch_samples),
+                start=torch.zeros((), dtype=torch.long),
+            ),
+            sum(
+                ((batch['contact_task_code'] == 2).sum() for batch in batch_samples),
+                start=torch.zeros((), dtype=torch.long),
+            ),
+            sum(
+                ((batch['contact_task_code'] == 0).sum() for batch in batch_samples),
+                start=torch.zeros((), dtype=torch.long),
+            ),
+            sum(
+                (batch['contact_task_code'].numel() for batch in batch_samples),
+                start=torch.zeros((), dtype=torch.long),
+            ),
+        ]).to(device=device, dtype=torch.long)
+        if self.args.average_tokens_across_devices and self.args.world_size > 1:
+            global_counts = self.accelerator.gather(local_counts).reshape(
+                -1, 6
+            ).sum(dim=0)
+        else:
+            global_counts = local_counts
+
+        geometry_start = int(
+            getattr(config, 'contact_geometry_start_blocks', 0)
+        )
+        geometry_ramp = max(
+            1, int(getattr(config, 'contact_geometry_ramp_blocks', 1))
+        )
+        geometry_scale = min(
+            1.0,
+            max(
+                0.0,
+                (self._seen_contact_blocks - geometry_start) / geometry_ramp,
+            ),
+        )
+        for batch in batch_samples:
+            batch['global_ce_tokens_in_window'] = global_counts[0].detach().cpu()
+            batch['global_contact_count_in_window'] = global_counts[1].detach().cpu()
+            batch['global_collision_valid_count_in_window'] = (
+                global_counts[2].detach().cpu()
+            )
+            batch['geometry_loss_scale'] = torch.tensor(
+                geometry_scale, dtype=torch.float32
+            )
+        self._seen_contact_blocks += int(global_counts[1].item())
+        self._last_window_counts = global_counts.detach().cpu()
+        self._last_geometry_scale = geometry_scale
+        return batch_samples, global_counts[0]
+
+    def _save_checkpoint(self, model, trial):
+        super()._save_checkpoint(model, trial)
+        if self.is_world_process_zero():
+            checkpoint_dir = os.path.join(
+                self.args.output_dir, f"checkpoint-{self.state.global_step}"
+            )
+            state_path = os.path.join(
+                checkpoint_dir, "grasp_contact_trainer_state.json"
+            )
+            with open(state_path, "w", encoding="utf-8") as handle:
+                json.dump(
+                    {
+                        "seen_contact_blocks": self._seen_contact_blocks,
+                        "training_phase": os.environ.get("CONTACT_PHASE"),
+                        "data_fingerprint": getattr(
+                            self.train_dataset, "data_fingerprint", None
+                        ),
+                    },
+                    handle,
+                )
+            export_locany_inference_files(checkpoint_dir)
+
+    def _load_from_checkpoint(self, resume_from_checkpoint, model=None):
+        super()._load_from_checkpoint(resume_from_checkpoint, model=model)
+        state_path = os.path.join(
+            resume_from_checkpoint, "grasp_contact_trainer_state.json"
+        )
+        if os.path.isfile(state_path):
+            with open(state_path, encoding="utf-8") as handle:
+                payload = json.load(handle)
+            saved_phase = payload.get("training_phase")
+            current_phase = os.environ.get("CONTACT_PHASE")
+            if saved_phase != current_phase:
+                raise ValueError(
+                    "checkpoint phase does not match exact resume phase: "
+                    f"saved={saved_phase!r}, current={current_phase!r}"
+                )
+            self._seen_contact_blocks = int(payload["seen_contact_blocks"])
+            previous_config_path = os.path.join(
+                resume_from_checkpoint, "config.json"
+            )
+            if os.path.isfile(previous_config_path):
+                with open(previous_config_path, encoding="utf-8") as handle:
+                    previous_config = json.load(handle)
+                previous_geometry_weight = sum(
+                    float(previous_config.get(name, 0.0))
+                    for name in (
+                        'contact_center_weight',
+                        'contact_angle_weight',
+                        'contact_width_weight',
+                    )
+                )
+                current_config = self.accelerator.unwrap_model(self.model).config
+                current_geometry_weight = sum(
+                    float(getattr(current_config, name, 0.0))
+                    for name in (
+                        'contact_center_weight',
+                        'contact_angle_weight',
+                        'contact_width_weight',
+                    )
+                )
+                if previous_geometry_weight == 0.0 and current_geometry_weight > 0.0:
+                    self._seen_contact_blocks = 0
+                    logger.info(
+                        "Starting a new geometry ramp because the resumed "
+                        "checkpoint had geometry weights disabled"
+                    )
+            logger.info(
+                "Restored seen_contact_blocks=%d",
+                self._seen_contact_blocks,
+            )
+
+    def _load_optimizer_and_scheduler(self, checkpoint):
+        super()._load_optimizer_and_scheduler(checkpoint)
+
+        forced_learning_rate = os.environ.get("LOCATE_FORCE_RESUME_LR")
+        if checkpoint is None or not forced_learning_rate:
+            return
+
+        learning_rate = float(forced_learning_rate)
+        for parameter_group in self.optimizer.param_groups:
+            parameter_group["lr"] = learning_rate
+            parameter_group["initial_lr"] = learning_rate
+
+        if self.lr_scheduler is not None:
+            group_count = len(self.optimizer.param_groups)
+            self.lr_scheduler.base_lrs = [learning_rate] * group_count
+            self.lr_scheduler._last_lr = [learning_rate] * group_count
+
+        logger.info(
+            "Forced resumed optimizer and scheduler learning rate to %.8g",
+            learning_rate,
+        )
     
     def training_step(self, model, inputs, num_items_in_batch=None):
         # 记录开始的step（用于resume时正确计算平均值）
@@ -1164,7 +2018,35 @@ class StreamPackingMTPTrainer(Trainer):
                                f"Total samples (this run) = {self._total_samples}, "
                                f"Avg samples/step = {avg_samples_per_step:.2f}")
         
-        return super().training_step(model, inputs, num_items_in_batch)
+        should_log_contact = (
+            self.state.global_step > 0
+            and self.state.global_step % self._sample_log_interval == 0
+            and self.state.global_step != self._last_contact_log_step
+            and self._last_window_counts is not None
+        )
+        if should_log_contact:
+            self._last_contact_log_step = self.state.global_step
+            counts = self._last_window_counts
+            logs = {
+                'contact/global_ce_tokens_in_window': float(counts[0]),
+                'contact/positive_blocks_in_window': float(counts[1]),
+                'contact/collision_valid_in_window': float(counts[2]),
+                'contact/negative_samples_in_window': float(counts[3]),
+                'contact/replay_samples_in_window': float(counts[4]),
+                'contact/samples_in_window': float(counts[5]),
+                'contact/seen_positive_blocks': float(self._seen_contact_blocks),
+                'contact/geometry_scale': float(self._last_geometry_scale),
+            }
+            logs.update(self._consume_loss_breakdown())
+            logs.update(self._consume_coordinate_metrics())
+            self.log(logs)
+
+        loss = super().training_step(model, inputs, num_items_in_batch)
+        unwrapped_model = self.accelerator.unwrap_model(model)
+        breakdown = getattr(unwrapped_model, '_last_loss_breakdown', {})
+        self._accumulate_coordinate_metrics(breakdown)
+        self._accumulate_loss_breakdown(breakdown, self.state.global_step)
+        return loss
     
     def get_train_dataloader(self):
         if self.train_dataset is None:
@@ -1212,6 +2094,9 @@ def build_stream_packed_dataset_mtp(
                 target_fps=data_args.target_fps,
                 max_frames=data_args.max_frames,
                 video_total_pixels=data_args.video_total_pixels,
+                max_contact_candidates=data_args.contact_max_candidates,
+                contact_collision_threshold=model_args.contact_collision_threshold,
+                contact_outside_threshold=model_args.contact_outside_threshold,
             )
             
             if len(ds) == 0:
@@ -1220,11 +2105,20 @@ def build_stream_packed_dataset_mtp(
             
             datasets.append(ds)
             
-            weight = repeat_time * len(ds) if repeat_time >= 1 else len(ds)
+            weight = meta.get('sampling_weight')
+            if weight is None:
+                weight = repeat_time * len(ds) if repeat_time >= 1 else len(ds)
+            weight = float(weight)
+            if weight <= 0:
+                raise ValueError(
+                    f'Dataset {ds_name} has non-positive sampling_weight={weight}'
+                )
             dataset_weights.append(weight)
             
-            logger.info(f'Added dataset: {ds_name}, length={len(ds)}, '
-                       f'repeat_time={repeat_time}, weight={weight:.0f}')
+            logger.info(
+                f'Added dataset: {ds_name}, length={len(ds)}, '
+                f'repeat_time={repeat_time}, weight={weight:.6g}'
+            )
             
         except Exception as e:
             traceback.print_exc()
@@ -1251,7 +2145,34 @@ def build_stream_packed_dataset_mtp(
 
 
 def main():
-    launcher = os.environ.get('LAUNCHER', 'slurm')
+    from eaglevl.model.locany.configuration_locateanything import (
+        LocateAnythingConfig,
+    )
+    from eaglevl.model.locany.modeling_locateanything import (
+        LocateAnythingForConditionalGeneration,
+    )
+    from eaglevl.model.moon_vit.modeling_vit import MoonVitPretrainedModel
+    from eaglevl.utils.locany.image_processing_locateanything import (
+        LocateAnythingImageProcessor,
+    )
+    from eaglevl.utils.locany.processing_locateanything import (
+        LocateAnythingProcessor,
+    )
+    from eaglevl.train.arguments import DataTrainingArguments, ModelArguments
+    from eaglevl.train.tools import (
+        MemoryLoggerCallback,
+        MilestoneCheckpointCallback,
+        SaveCheckpointCallback,
+        load_config,
+    )
+    from eaglevl.train.trainer_monkey_patch import (
+        replace_create_optimizer_with_various_lr,
+    )
+    from eaglevl.patch.train_dataloader_patch import replace_train_dataloader
+    from eaglevl.patch.train_sampler_patch import replace_train_sampler
+
+    _install_training_patches()
+    launcher = os.environ.get('LAUNCHER', 'pytorch')
     init_dist(launcher=launcher, backend='nccl')
     parser = HfArgumentParser((ModelArguments, DataTrainingArguments, TrainingArguments))
     
@@ -1260,6 +2181,28 @@ def main():
     else:
         model_args, data_args, training_args = parser.parse_args_into_dataclasses()
 
+    if model_args.contact_loss_enabled:
+        if model_args.block_size != 6:
+            raise ValueError("contact_loss_enabled requires --block_size 6")
+        if model_args.contact_geometry_start_blocks < 0:
+            raise ValueError("contact_geometry_start_blocks must be non-negative")
+        if model_args.contact_geometry_ramp_blocks <= 0:
+            raise ValueError("contact_geometry_ramp_blocks must be positive")
+        for name, value in (
+            ('contact_coord_mass_threshold', model_args.contact_coord_mass_threshold),
+            ('contact_coord_entropy_threshold', model_args.contact_coord_entropy_threshold),
+            ('contact_collision_threshold', model_args.contact_collision_threshold),
+            ('contact_outside_threshold', model_args.contact_outside_threshold),
+        ):
+            if not 0.0 <= value <= 1.0:
+                raise ValueError(f"{name} must be in [0, 1]")
+        if not training_args.average_tokens_across_devices:
+            logger.warning(
+                "Enabling average_tokens_across_devices for globally normalized "
+                "contact training"
+            )
+            training_args.average_tokens_across_devices = True
+
     if os.path.exists(osp.join(training_args.output_dir, 'done.txt')):
         logger.info("Training done (done.txt exists), exiting!")
         return
@@ -1292,12 +2235,27 @@ def main():
     )
     logger.info(f'Training parameters: {training_args}')
 
-    # Checkpoint detection
-    last_checkpoint = None
-    if os.path.isdir(training_args.output_dir) and training_args.do_train and not training_args.overwrite_output_dir:
-        last_checkpoint = get_last_checkpoint_guard(training_args.output_dir)
-        if last_checkpoint is not None and training_args.resume_from_checkpoint is None:
-            logger.info(f'Checkpoint detected at {last_checkpoint}, resuming training.')
+    # Never infer resume intent from output_dir. Phase transitions deliberately
+    # reuse model weights with a fresh optimizer/data stream, while exact
+    # same-phase resume must pass the explicit state/fingerprint gates.
+    if (
+        os.path.isdir(training_args.output_dir)
+        and training_args.do_train
+        and not training_args.overwrite_output_dir
+        and training_args.resume_from_checkpoint is None
+    ):
+        existing_checkpoints = [
+            entry
+            for entry in os.listdir(training_args.output_dir)
+            if entry.startswith('checkpoint-')
+            and os.path.isdir(os.path.join(training_args.output_dir, entry))
+        ]
+        if existing_checkpoints:
+            raise ValueError(
+                "output_dir already contains checkpoints but no explicit "
+                "RESUME_FROM_CHECKPOINT was provided: "
+                f"{sorted(existing_checkpoints)}"
+            )
             
     set_seed(training_args.seed)
     
@@ -1308,7 +2266,11 @@ def main():
         tokenizer_path, add_eos_token=False, trust_remote_code=True, use_fast=False)
     tokenizer.tokenizer_path = tokenizer_path
     tokenizer.model_max_length = data_args.max_seq_length
-    num_new_tokens = tokenizer.add_tokens(special_tokens_list + number_tokens_list, special_tokens=True)
+    grasp_tokens = ["<grasp>", "</grasp>"]
+    num_new_tokens = tokenizer.add_tokens(
+        special_tokens_list + grasp_tokens + number_tokens_list,
+        special_tokens=True,
+    )
     
     if len(tokenizer.encode("assistant")) > 1:
         tokenizer.add_tokens(["assistant"], special_tokens=False)
@@ -1323,9 +2285,26 @@ def main():
     ref_end_token_id = tokenizer.convert_tokens_to_ids(REF_END_TOKEN)
     coord_start_token_id = tokenizer.convert_tokens_to_ids(number_tokens_list[0])
     coord_end_token_id = tokenizer.convert_tokens_to_ids(number_tokens_list[-1])
+    grasp_start_token_id = tokenizer.convert_tokens_to_ids(grasp_tokens[0])
+    grasp_end_token_id = tokenizer.convert_tokens_to_ids(grasp_tokens[1])
+    coordinate_token_ids = tokenizer.convert_tokens_to_ids(number_tokens_list)
+    expected_coordinate_ids = list(
+        range(coord_start_token_id, coord_start_token_id + len(number_tokens_list))
+    )
+    if coordinate_token_ids != expected_coordinate_ids:
+        raise ValueError(
+            "LocateAnything contact loss requires contiguous <0> through <1000> token IDs"
+        )
     none_token_ids = tokenizer.encode("none", add_special_tokens=False)
     none_token_id = none_token_ids[0] if len(none_token_ids) == 1 else 4064
-    
+
+    from eaglevl.train.grasp_contact import (
+        configure_llm_lora,
+        enable_task_token_training,
+        initialize_missing_task_adapters,
+    )
+    model_loading_info = {"missing_keys": []}
+
     if model_args.model_name_or_path is not None:
         # ===== LocateAnything (MoonVit + Qwen2/Qwen3) Loading Path =====
         logger.info('Loading LocateAnythingForConditionalGeneration...')
@@ -1334,9 +2313,9 @@ def main():
         config._attn_implementation_autoset = False
         config.text_config._attn_implementation = model_args.attn_implementation
         config.text_config._attn_implementation_autoset = False
-        config.vision_config._attn_implementation = 'flash_attention_2'
+        config.vision_config._attn_implementation = model_args.attn_implementation
         config.vision_config._attn_implementation_autoset = False
-        logger.info(f'Text attn: {model_args.attn_implementation}, Vision attn: flash_attention_2')
+        logger.info(f'Text attn: {model_args.attn_implementation}, Vision attn: {model_args.attn_implementation}')
 
         config.image_token_index = image_token_index
         config.text_config.block_size = int(model_args.block_size)
@@ -1350,11 +2329,28 @@ def main():
         config.ref_start_token_id = ref_start_token_id
         config.ref_end_token_id = ref_end_token_id
         config.none_token_id = none_token_id
-
-        model = LocateAnythingForConditionalGeneration.from_pretrained(
+        config.grasp_task_token_ids = [
+            grasp_start_token_id,
+            grasp_end_token_id,
+        ]
+        config.contact_loss_enabled = model_args.contact_loss_enabled
+        config.contact_pair_weight = model_args.contact_pair_weight
+        config.contact_center_weight = model_args.contact_center_weight
+        config.contact_angle_weight = model_args.contact_angle_weight
+        config.contact_width_weight = model_args.contact_width_weight
+        config.contact_coord_mass_threshold = model_args.contact_coord_mass_threshold
+        config.contact_coord_entropy_threshold = model_args.contact_coord_entropy_threshold
+        config.contact_minimum_width_diagonal = model_args.contact_minimum_width_diagonal
+        config.contact_collision_threshold = model_args.contact_collision_threshold
+        config.contact_outside_threshold = model_args.contact_outside_threshold
+        config.contact_geometry_start_blocks = model_args.contact_geometry_start_blocks
+        config.contact_geometry_ramp_blocks = model_args.contact_geometry_ramp_blocks
+
+        model, model_loading_info = LocateAnythingForConditionalGeneration.from_pretrained(
             model_args.model_name_or_path, 
             torch_dtype=torch.bfloat16, config=config, 
-            attn_implementation=model_args.attn_implementation
+            attn_implementation=model_args.attn_implementation,
+            output_loading_info=True,
         )
             
         model.text_mask_token_id = text_mask_token_id
@@ -1379,7 +2375,7 @@ def main():
 
         if vision_config.model_type == 'moonvit':
             logger.info('Loading MoonVit...')
-            vision_config._attn_implementation = 'flash_attention_2'
+            vision_config._attn_implementation = model_args.attn_implementation
             vision_model = MoonVitPretrainedModel.from_pretrained(
                 model_args.vision_path, torch_dtype=torch.bfloat16, config=vision_config)
         else:
@@ -1387,7 +2383,7 @@ def main():
             
         logger.info('Loading LLM...')
         text_config = AutoConfig.from_pretrained(model_args.llm_path, trust_remote_code=True)
-        text_config._attn_implementation = 'magi'
+        text_config._attn_implementation = model_args.attn_implementation
 
         llm = AutoModelForCausalLM.from_pretrained(
             model_args.llm_path, torch_dtype=torch.bfloat16,
@@ -1397,7 +2393,30 @@ def main():
             vision_config.to_dict(), text_config.to_dict(), 
             image_token_index=image_token_index, 
             mlp_connector_layers=model_args.mlp_connector_layers)
-        locateanything_config._attn_implementation = 'magi'
+        locateanything_config._attn_implementation = model_args.attn_implementation
+        locateanything_config.box_start_token_id = box_start_token_id
+        locateanything_config.box_end_token_id = box_end_token_id
+        locateanything_config.coord_start_token_id = coord_start_token_id
+        locateanything_config.coord_end_token_id = coord_end_token_id
+        locateanything_config.ref_start_token_id = ref_start_token_id
+        locateanything_config.ref_end_token_id = ref_end_token_id
+        locateanything_config.none_token_id = none_token_id
+        locateanything_config.grasp_task_token_ids = [
+            grasp_start_token_id,
+            grasp_end_token_id,
+        ]
+        locateanything_config.contact_loss_enabled = model_args.contact_loss_enabled
+        locateanything_config.contact_pair_weight = model_args.contact_pair_weight
+        locateanything_config.contact_center_weight = model_args.contact_center_weight
+        locateanything_config.contact_angle_weight = model_args.contact_angle_weight
+        locateanything_config.contact_width_weight = model_args.contact_width_weight
+        locateanything_config.contact_coord_mass_threshold = model_args.contact_coord_mass_threshold
+        locateanything_config.contact_coord_entropy_threshold = model_args.contact_coord_entropy_threshold
+        locateanything_config.contact_minimum_width_diagonal = model_args.contact_minimum_width_diagonal
+        locateanything_config.contact_collision_threshold = model_args.contact_collision_threshold
+        locateanything_config.contact_outside_threshold = model_args.contact_outside_threshold
+        locateanything_config.contact_geometry_start_blocks = model_args.contact_geometry_start_blocks
+        locateanything_config.contact_geometry_ramp_blocks = model_args.contact_geometry_ramp_blocks
         model = LocateAnythingForConditionalGeneration(locateanything_config, vision_model, llm)
 
         chat_template_data = load_config(model_args.chat_template_path)
@@ -1406,7 +2425,16 @@ def main():
         image_processor = LocateAnythingImageProcessor(**preprocessor_config)
         processor_config["chat_template"] = chat_template_data["chat_template"]
         processor = LocateAnythingProcessor(tokenizer=tokenizer, image_processor=image_processor, **processor_config)
-        
+
+    initialized_task_parameters = initialize_missing_task_adapters(
+        model, model_loading_info.get("missing_keys", [])
+    )
+    if initialized_task_parameters:
+        logger.info(
+            "Zero-initialized %d task-adapter parameters absent from MODEL_PATH",
+            initialized_task_parameters,
+        )
+
     model.neftune_alpha = data_args.neftune_alpha
     
     # Enable packing mode for stream packing (works for both pretrained and scratch models)
@@ -1423,6 +2451,24 @@ def main():
         output_embeddings = model.language_model.get_output_embeddings().weight.data
         output_embeddings_avg = output_embeddings[:-num_new_tokens].mean(dim=0, keepdim=True)
         output_embeddings[-num_new_tokens:] = output_embeddings_avg
+        input_embeddings = model.language_model.get_input_embeddings().weight.data
+        input_embeddings[grasp_start_token_id] = (
+            0.9 * input_embeddings[box_start_token_id]
+            + 0.1 * input_embeddings[ref_start_token_id]
+        )
+        input_embeddings[grasp_end_token_id] = (
+            0.9 * input_embeddings[box_end_token_id]
+            + 0.1 * input_embeddings[ref_end_token_id]
+        )
+        if output_embeddings.data_ptr() != input_embeddings.data_ptr():
+            output_embeddings[grasp_start_token_id] = (
+                0.9 * output_embeddings[box_start_token_id]
+                + 0.1 * output_embeddings[ref_start_token_id]
+            )
+            output_embeddings[grasp_end_token_id] = (
+                0.9 * output_embeddings[box_end_token_id]
+                + 0.1 * output_embeddings[ref_end_token_id]
+            )
         model.config.text_config.vocab_size = len(tokenizer)
         model.language_model.config.vocab_size = len(tokenizer)
         dist.barrier()
@@ -1470,15 +2516,27 @@ def main():
         _freeze_params(model.language_model)
 
     if model_args.unfreeze_lm_head:
-        model.language_model.lm_head.requires_grad = True
+        for param in model.language_model.lm_head.parameters():
+            param.requires_grad = True
 
     if model_args.use_backbone_lora:
         model.wrap_backbone_lora(r=model_args.use_backbone_lora, lora_alpha=2 * model_args.use_backbone_lora)
         model.config.use_backbone_lora = model_args.use_backbone_lora
 
     if model_args.use_llm_lora:
-        model.wrap_llm_lora(r=model_args.use_llm_lora, lora_alpha=2 * model_args.use_llm_lora)
-        model.config.use_llm_lora = model_args.use_llm_lora
+        installed = configure_llm_lora(model, model_args.use_llm_lora)
+        logger.info(
+            "%s LLM LoRA rank=%d",
+            "Installed" if installed else "Reused",
+            model_args.use_llm_lora,
+        )
+        task_token_parameters = enable_task_token_training(
+            model, [grasp_start_token_id, grasp_end_token_id]
+        )
+        logger.info(
+            "Enabled compact grasp-token embedding adapter with %d parameters",
+            task_token_parameters,
+        )
 
     if model_args.freeze_mlp:
         _freeze_params(model.mlp1)
@@ -1544,7 +2602,7 @@ def main():
 
     # Training
     if training_args.do_train:
-        checkpoint = training_args.resume_from_checkpoint or last_checkpoint
+        checkpoint = training_args.resume_from_checkpoint
         
         if checkpoint is not None:
             training_args.ignore_data_skip = True
@@ -1554,49 +2612,27 @@ def main():
             dataloader_state_path = os.path.join(checkpoint, f"dataloader_state_rank{rank}.pt")
             
             if os.path.exists(dataloader_state_path):
-                try:
-                    dataloader_state = torch.load(dataloader_state_path, weights_only=False)
-                    train_dataset.load_state_dict(dataloader_state)
-                    logger.info(f"Rank {rank}: Loaded dataloader state from {dataloader_state_path}")
-                except Exception as e:
-                    logger.warning(f"Rank {rank}: Failed to load dataloader state: {e}")
-                    traceback.print_exc()
+                dataloader_state = torch.load(
+                    dataloader_state_path, weights_only=False
+                )
+                train_dataset.load_state_dict(dataloader_state)
+                logger.info(
+                    "Rank %s: Loaded dataloader state from %s",
+                    rank,
+                    dataloader_state_path,
+                )
             else:
-                logger.warning(f"Rank {rank}: No dataloader state found at {dataloader_state_path}")
+                raise FileNotFoundError(
+                    f"Rank {rank}: no dataloader state at {dataloader_state_path}; "
+                    "use MODEL_PATH for a fresh data stream instead of resume"
+                )
 
         train_result = trainer.train(resume_from_checkpoint=checkpoint)
         trainer.save_model()
 
         if get_rank() == 0:
             output_dir = training_args.output_dir
-
-            locany_utils_src = osp.join(osp.dirname(osp.dirname(osp.abspath(__file__))), 'utils', 'locany')
-            skip_files = {'config.json', 'README.md', '__init__.py', '__pycache__'}
-            if osp.isdir(locany_utils_src):
-                for file in os.listdir(locany_utils_src):
-                    if file in skip_files or file.startswith('__'):
-                        continue
-                    src_file = osp.join(locany_utils_src, file)
-                    dst_file = osp.join(output_dir, file)
-                    if osp.isfile(src_file):
-                        shutil.copy2(src_file, dst_file)
-                logger.info(f"Copied inference files from {locany_utils_src} to {output_dir}")
-
-            config_path = osp.join(output_dir, 'config.json')
-            if osp.exists(config_path):
-                with open(config_path, 'r', encoding='utf-8') as f:
-                    config_data = json.load(f)
-                config_data['auto_map'] = {
-                    "AutoConfig": "configuration_locateanything.LocateAnythingConfig",
-                    "AutoModel": "modeling_locateanything.LocateAnythingForConditionalGeneration",
-                    "AutoModelForCausalLM": "modeling_locateanything.LocateAnythingForConditionalGeneration",
-                    "AutoImageProcessor": "image_processing_locateanything.LocateAnythingImageProcessor",
-                    "AutoProcessor": "processing_locateanything.LocateAnythingProcessor",
-                }
-                with open(config_path, 'w', encoding='utf-8') as f:
-                    json.dump(config_data, f, indent=2, ensure_ascii=False)
-                    f.write('\n')
-                logger.info("Updated config.json with auto_map")
+            export_locany_inference_files(output_dir)
 
         metrics = train_result.metrics
         metrics['train_samples'] = 'streaming'
diff --git a/Embodied/eaglevl/utils/locany/attn_mask_utils.py b/Embodied/eaglevl/utils/locany/attn_mask_utils.py
index c75071a..1b0a4a9 100644
--- a/Embodied/eaglevl/utils/locany/attn_mask_utils.py
+++ b/Embodied/eaglevl/utils/locany/attn_mask_utils.py
@@ -177,6 +177,9 @@ def create_block_diff_mask_by_pe_4d(
     block_prefix = (~x0_flag_q & x0_flag_kv) & (kv_idx < prefix_len)
 
     final_mask = (block_causal | block_mutual | block_prefix)
+    if not final_mask.any(dim=-1).all():
+        bad_rows = (~final_mask.any(dim=-1)).nonzero(as_tuple=False).flatten()
+        raise ValueError(f"MTP attention mask contains fully masked rows: {bad_rows[:8].tolist()}")
     customized_mask = torch.full_like(final_mask, float('-inf'), dtype=torch.bfloat16)
     customized_mask.masked_fill_(final_mask, 0.0)
 
diff --git a/Embodied/eaglevl/utils/locany/generate_utils.py b/Embodied/eaglevl/utils/locany/generate_utils.py
index 4b40743..2614ffb 100644
--- a/Embodied/eaglevl/utils/locany/generate_utils.py
+++ b/Embodied/eaglevl/utils/locany/generate_utils.py
@@ -26,6 +26,12 @@ def get_token_ids_from_config(config) -> Dict[str, int]:
     # Get from main config
     token_ids['box_start_token_id'] = getattr(config, 'box_start_token_id', 151668)
     token_ids['box_end_token_id'] = getattr(config, 'box_end_token_id', 151669)
+    token_ids['grasp_start_token_id'] = getattr(
+        config, 'grasp_start_token_id', token_ids['box_start_token_id']
+    )
+    token_ids['grasp_end_token_id'] = getattr(
+        config, 'grasp_end_token_id', token_ids['box_end_token_id']
+    )
     token_ids['coord_start_token_id'] = getattr(config, 'coord_start_token_id', 151677)
     token_ids['coord_end_token_id'] = getattr(config, 'coord_end_token_id', 152677)
     token_ids['ref_start_token_id'] = getattr(config, 'ref_start_token_id', 151672)
@@ -174,10 +180,37 @@ def sample_tokens(
     fallback_box = torch.zeros(1, dtype=x0.dtype, device=x0.device)
 
     for b in range(batch_size):
-        decoded_box = decode_bbox_avg(
-            logits[b], probs[b], token_ids, keep_k=generate_kwargs.get('keep_k_avg', 4),
-            generation_mode=generate_kwargs.get('generation_mode', 'hybrid'),
+        geometry_type = generate_kwargs.get('geometry_type', 'bbox')
+        contact_frame_expected = (
+            geometry_type == 'contact'
+            and generated[b, -1].item() == token_ids['ref_end_token_id']
         )
+        if contact_frame_expected:
+            decoded_box = decode_contact_pair(
+                logits[b],
+                probs[b],
+                token_ids,
+                keep_k=generate_kwargs.get('contact_keep_k', 4),
+                image_size=generate_kwargs.get('image_size'),
+                minimum_width_diagonal=generate_kwargs.get(
+                    'contact_minimum_width_diagonal', 1e-4
+                ),
+                maximum_width_diagonal=generate_kwargs.get(
+                    'contact_maximum_width_diagonal', 1.0
+                ),
+                coord_mass_threshold=generate_kwargs.get(
+                    'contact_coord_mass_threshold', 1e-4
+                ),
+                force_frame=True,
+            )
+        elif geometry_type == 'contact':
+            decoded_box = None
+        else:
+            decoded_box = decode_bbox_avg(
+                logits[b], probs[b], token_ids,
+                keep_k=generate_kwargs.get('keep_k_avg', 4),
+                generation_mode=generate_kwargs.get('generation_mode', 'hybrid'),
+            )
         if decoded_box is not None:
             box_avg.append(decoded_box)
         else:
@@ -192,6 +225,169 @@ def sample_tokens(
     return probs, confidence, x0, box_avg
 
 
+def decode_contact_pair(
+    logits,
+    probs,
+    token_ids: Dict[str, int],
+    keep_k=4,
+    start_thresh=0.7,
+    end_thresh=0.2,
+    image_size=None,
+    minimum_width_diagonal=1e-4,
+    maximum_width_diagonal=1.0,
+    coord_mass_threshold=1e-4,
+    force_frame=False,
+):
+    """Jointly decode four contact coordinates under image-space constraints."""
+    del logits
+    coord_start = token_ids['coord_start_token_id']
+    coord_end = token_ids['coord_end_token_id']
+    grasp_start = token_ids.get(
+        'grasp_start_token_id', token_ids['box_start_token_id']
+    )
+    grasp_end = token_ids.get(
+        'grasp_end_token_id', token_ids['box_end_token_id']
+    )
+    none_token = token_ids['none_token_id']
+    null_token = token_ids['null_token_id']
+
+    if coord_end - coord_start != 1000:
+        raise ValueError('contact decoding requires 1001 contiguous coordinate tokens')
+    if not force_frame and probs[0, grasp_start] < start_thresh:
+        return None
+    if force_frame and probs[1, none_token] >= probs[
+        1, coord_start : coord_end + 1
+    ].max():
+        return torch.tensor(
+            [grasp_start, none_token, grasp_end, null_token, null_token, null_token],
+            dtype=torch.long,
+            device=probs.device,
+        )
+    contact_token_ids = dict(token_ids)
+    contact_token_ids['box_start_token_id'] = grasp_start
+    contact_token_ids['box_end_token_id'] = grasp_end
+    box_type = is_valid_box_frame(
+        probs,
+        contact_token_ids,
+        start_thresh=0.0 if force_frame else start_thresh,
+        end_thresh=0.0 if force_frame else end_thresh,
+        topk=keep_k,
+    )
+    if box_type == 'empty_box':
+        return torch.tensor(
+            [grasp_start, none_token, grasp_end, null_token, null_token, null_token],
+            dtype=torch.long,
+            device=probs.device,
+        )
+    if box_type == 'illegal_box':
+        return None
+
+    coordinate_probs = probs[1:5, coord_start : coord_end + 1]
+    coordinate_mass = coordinate_probs.sum(dim=-1)
+    if (coordinate_mass < coord_mass_threshold).any():
+        return None
+    keep_k = max(1, min(int(keep_k), coordinate_probs.shape[-1]))
+    top_probs, top_values = coordinate_probs.topk(keep_k, dim=-1)
+
+    choice_axis = torch.arange(keep_k, device=probs.device)
+    combinations = torch.cartesian_prod(
+        choice_axis, choice_axis, choice_axis, choice_axis
+    )
+    if combinations.ndim == 1:
+        combinations = combinations.unsqueeze(0)
+    positions = torch.arange(4, device=probs.device).unsqueeze(1)
+    choices = combinations.transpose(0, 1)
+    candidate_values = top_values[positions, choices].transpose(0, 1).float()
+    candidate_log_scores = (
+        top_probs[positions, choices].clamp_min(1e-30).log().sum(dim=0)
+    )
+
+    if image_size is None:
+        image_width = image_height = 1.0
+    else:
+        size = torch.as_tensor(image_size).flatten()
+        if size.numel() != 2:
+            raise ValueError('image_size must be (width, height) for contact decoding')
+        image_width = float(size[0].item())
+        image_height = float(size[1].item())
+        if image_width <= 0 or image_height <= 0:
+            raise ValueError('image_size values must be positive')
+
+    dx = (candidate_values[:, 2] - candidate_values[:, 0]) * image_width
+    dy = (candidate_values[:, 3] - candidate_values[:, 1]) * image_height
+    width_diagonal = torch.sqrt(dx.square() + dy.square()) / (
+        1000.0 * (image_width ** 2 + image_height ** 2) ** 0.5
+    )
+    valid = (
+        (width_diagonal >= float(minimum_width_diagonal))
+        & (width_diagonal <= float(maximum_width_diagonal))
+    )
+    if not valid.any():
+        return None
+    candidate_log_scores = candidate_log_scores.masked_fill(~valid, -torch.inf)
+    best_values = candidate_values[candidate_log_scores.argmax()].long()
+    return torch.cat(
+        (
+            best_values.new_tensor([grasp_start]),
+            best_values + coord_start,
+            best_values.new_tensor([grasp_end]),
+        )
+    )
+
+
+def constrain_contact_ar_token(next_token_logits, generated, token_ids):
+    """Apply the dedicated contact vocabulary mask to one AR decoding slot."""
+    grasp_start = token_ids.get(
+        'grasp_start_token_id', token_ids['box_start_token_id']
+    )
+    grasp_end = token_ids.get(
+        'grasp_end_token_id', token_ids['box_end_token_id']
+    )
+    coord_start = token_ids['coord_start_token_id']
+    coord_end = token_ids['coord_end_token_id']
+    none_token = token_ids['none_token_id']
+    ref_end = token_ids['ref_end_token_id']
+    sequence = generated[0].tolist()
+
+    def forced(token_id, out_type):
+        return out_type, torch.tensor(
+            [token_id], dtype=generated.dtype, device=generated.device
+        )
+
+    if not sequence:
+        return None
+
+    last_grasp_start = max(
+        (index for index, token in enumerate(sequence) if token == grasp_start),
+        default=-1,
+    )
+    last_grasp_end = max(
+        (index for index, token in enumerate(sequence) if token == grasp_end),
+        default=-1,
+    )
+    if sequence[-1] == ref_end and last_grasp_start <= last_grasp_end:
+        return forced(grasp_start, 'continue_ar')
+    if last_grasp_start <= last_grasp_end:
+        return None
+
+    content = sequence[last_grasp_start + 1 :]
+    if content and content[0] == none_token:
+        return forced(grasp_end, 'box_end_ar')
+    coordinate_count = sum(
+        coord_start <= token <= coord_end for token in content
+    )
+    if coordinate_count >= 4:
+        return forced(grasp_end, 'box_end_ar')
+    if any(not coord_start <= token <= coord_end for token in content):
+        return forced(grasp_end, 'box_end_ar')
+
+    coord_logits = next_token_logits[0, 0, coord_start : coord_end + 1]
+    coord_token = int(coord_logits.argmax().item()) + coord_start
+    if not content and next_token_logits[0, 0, none_token] >= coord_logits.max():
+        return forced(none_token, 'coord_ar')
+    return forced(coord_token, 'coord_ar')
+
+
 def sample_tokens_ar(
     logits: torch.Tensor,
     generated: torch.Tensor,
@@ -405,7 +601,12 @@ def decode_ref(
     return torch.cat([start_t, final_text_ids])
 
 
-def handle_pattern(x0, token_ids: Dict[str, int], generation_mode: str = 'hybrid'):
+def handle_pattern(
+    x0,
+    token_ids: Dict[str, int],
+    generation_mode: str = 'hybrid',
+    geometry_type: str = 'bbox',
+):
     """
     Args:
         x0: Token ID list of length 6
@@ -413,8 +614,16 @@ def handle_pattern(x0, token_ids: Dict[str, int], generation_mode: str = 'hybrid
     """
     null_token_id = token_ids['null_token_id']
     im_end_token_id = token_ids['im_end_token_id']
-    box_start_token_id = token_ids['box_start_token_id']
-    box_end_token_id = token_ids['box_end_token_id']
+    box_start_token_id = (
+        token_ids.get('grasp_start_token_id', token_ids['box_start_token_id'])
+        if geometry_type == 'contact'
+        else token_ids['box_start_token_id']
+    )
+    box_end_token_id = (
+        token_ids.get('grasp_end_token_id', token_ids['box_end_token_id'])
+        if geometry_type == 'contact'
+        else token_ids['box_end_token_id']
+    )
     none_token_id = token_ids['none_token_id']
     coord_start_token_id = token_ids['coord_start_token_id']
     coord_end_token_id = token_ids['coord_end_token_id']
@@ -451,10 +660,10 @@ def handle_pattern(x0, token_ids: Dict[str, int], generation_mode: str = 'hybrid
             else:
                 break
 
-        # Standard 4-coordinate bbox: <box><x1><x2><y1><y2></box>
+        # Four-coordinate bbox or contact pair, selected by the caller's task.
         if coord_ix == 5 and x0[5] == box_end_token_id:
             return {
-                "type": "coord_box",
+                "type": "contact_box" if geometry_type == 'contact' else "coord_box",
                 "tokens": x0,
                 "need_switch_to_ar": False,
                 "is_terminal": False,
@@ -462,7 +671,11 @@ def handle_pattern(x0, token_ids: Dict[str, int], generation_mode: str = 'hybrid
         # Two-coordinate pointing: <box><x><y></box>
         # Convention: the first two coordinates are valid coord tokens, the third token is box_end.
         # Remaining positions (if any) are not part of the pattern; truncate at box_end.
-        elif coord_ix == 3 and x0[3] == box_end_token_id:
+        elif (
+            geometry_type != 'contact'
+            and coord_ix == 3
+            and x0[3] == box_end_token_id
+        ):
             return {
                 "type": "point_box",
                 "tokens": x0[:4],
@@ -471,6 +684,13 @@ def handle_pattern(x0, token_ids: Dict[str, int], generation_mode: str = 'hybrid
             }
         else:
             if generation_mode == 'fast':
+                if geometry_type == 'contact':
+                    return {
+                        "type": "contact_decode_error",
+                        "tokens": [box_start_token_id, box_end_token_id],
+                        "need_switch_to_ar": False,
+                        "is_terminal": True,
+                    }
                 # fast mode: treat as coord_box, stay in MTP
                 return {
                     "type": "coord_box",
@@ -501,4 +721,4 @@ def handle_pattern(x0, token_ids: Dict[str, int], generation_mode: str = 'hybrid
             "tokens": x0,
             "need_switch_to_ar": False,
             "is_terminal": False,
-        }
\ No newline at end of file
+        }
diff --git a/Embodied/eaglevl/utils/locany/modeling_locateanything.py b/Embodied/eaglevl/utils/locany/modeling_locateanything.py
index 5dad399..cfa2a04 100644
--- a/Embodied/eaglevl/utils/locany/modeling_locateanything.py
+++ b/Embodied/eaglevl/utils/locany/modeling_locateanything.py
@@ -10,6 +10,7 @@ from typing import List, Optional, Tuple, Union
 import numpy as np
 import torch
 from torch import nn
+import torch.nn.functional as F
 from torch.nn import CrossEntropyLoss
 from transformers.generation import GenerationMixin
 from transformers.modeling_outputs import CausalLMOutputWithPast
@@ -26,10 +27,12 @@ from .mask_magi_utils import *
 from .configuration_qwen2 import Qwen2Config
 
 from .generate_utils import (
+    constrain_contact_ar_token,
     sample_tokens,
     handle_pattern,
     get_token_ids_from_config,
 )
+from .grasp_adapter_utils import apply_grasp_task_output_delta
 
 logger = logging.get_logger(__name__)
 
@@ -129,6 +132,25 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
             else:
                 raise ValueError(f'Unsupported language model architecture: {config.text_config.architectures[0]}. Only Qwen2ForCausalLM and Qwen3ForCausalLM are supported.')
 
+        task_token_ids = list(getattr(config, 'grasp_task_token_ids', []))
+        self.register_buffer(
+            '_grasp_task_token_ids',
+            torch.tensor(task_token_ids, dtype=torch.long),
+            persistent=False,
+        )
+        self.grasp_task_embedding_delta = (
+            nn.Parameter(torch.zeros(len(task_token_ids), config.text_config.hidden_size))
+            if task_token_ids
+            else None
+        )
+        self.grasp_task_output_delta = (
+            nn.Parameter(torch.zeros(len(task_token_ids), config.text_config.hidden_size))
+            if task_token_ids
+            else None
+        )
+        self._grasp_task_embedding_hook = None
+        self._grasp_task_output_hook = None
+
         vit_hidden_size = config.vision_config.hidden_size
         llm_hidden_size = config.text_config.hidden_size
 
@@ -148,6 +170,7 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
         self.use_llm_lora = config.use_llm_lora
         if config.use_llm_lora:
             self.wrap_llm_lora(r=config.use_llm_lora, lora_alpha=2 * config.use_llm_lora)
+        self.register_grasp_task_embedding_hook()
 
         self.token_ids = get_token_ids_from_config(config)
 
@@ -158,7 +181,48 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
         else:
             self._no_split_modules = ["Qwen2DecoderLayer"]
 
-        
+    def register_grasp_task_embedding_hook(self):
+        """Inject compact trainable input/output deltas for grasp tokens."""
+        if self._grasp_task_embedding_hook is not None:
+            self._grasp_task_embedding_hook.remove()
+            self._grasp_task_embedding_hook = None
+        if self._grasp_task_output_hook is not None:
+            self._grasp_task_output_hook.remove()
+            self._grasp_task_output_hook = None
+        if self.grasp_task_embedding_delta is None:
+            return 0
+
+        def add_task_delta(_module, inputs, output):
+            token_ids = inputs[0]
+            result = output
+            delta = self.grasp_task_embedding_delta.to(dtype=output.dtype)
+            for row, token_id in enumerate(self._grasp_task_token_ids):
+                token_mask = (token_ids == token_id).unsqueeze(-1)
+                result = torch.where(token_mask, result + delta[row], result)
+            return result
+
+        embedding = self.language_model.get_input_embeddings()
+        self._grasp_task_embedding_hook = embedding.register_forward_hook(
+            add_task_delta
+        )
+
+        def add_task_logits(_module, inputs, output):
+            return apply_grasp_task_output_delta(
+                inputs[0],
+                output,
+                self._grasp_task_token_ids,
+                self.grasp_task_output_delta,
+            )
+
+        lm_head = self.language_model.get_output_embeddings()
+        self._grasp_task_output_hook = lm_head.register_forward_hook(
+            add_task_logits
+        )
+        return int(
+            self.grasp_task_embedding_delta.numel()
+            + self.grasp_task_output_delta.numel()
+        )
+
     def wrap_backbone_lora(self, r=128, lora_alpha=256, lora_dropout=0.05):
         lora_config = LoraConfig(
             r=r,
@@ -353,6 +417,13 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
         generation_mode = generate_kwargs.get('generation_mode', 'hybrid')
         assert generation_mode in ('fast', 'slow', 'hybrid'), \
             f"Unsupported generation_mode='{generation_mode}'. Use 'fast', 'slow', or 'hybrid'."
+        geometry_type = generate_kwargs.get('geometry_type', 'bbox')
+        if geometry_type not in ('bbox', 'contact'):
+            raise ValueError(
+                f"Unsupported geometry_type={geometry_type!r}; use 'bbox' or 'contact'."
+            )
+        if geometry_type == 'contact' and n_future_tokens != 6:
+            raise ValueError('contact decoding requires n_future_tokens=6')
 
         sampling_history = []
 
@@ -422,7 +493,12 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
             is_box_empty = (box_avg[0] == 0).all()
             new_tokens = x0[0] if is_box_empty else box_avg[0]
 
-            out_pattern = handle_pattern(new_tokens, self.token_ids, generation_mode)
+            out_pattern = handle_pattern(
+                new_tokens,
+                self.token_ids,
+                generation_mode,
+                geometry_type=geometry_type,
+            )
             out_type = out_pattern['type']
             out_token = torch.tensor(out_pattern['tokens'], dtype=x0.dtype, device=x0.device)
 
@@ -432,6 +508,13 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
         def _sample_token_in_ar(generated, outputs):
             """Sample a single token using AR (Auto-Regressive) mode."""
             next_token_logits = outputs.logits[:, -1:, :]
+            if geometry_type == 'contact':
+                constrained = constrain_contact_ar_token(
+                    next_token_logits, generated[:, seq_len:], self.token_ids
+                )
+                if constrained is not None:
+                    return constrained
+
             probs, confidence, x0, _ = sample_tokens(
                 next_token_logits, generated, self.token_ids, **generate_kwargs
             )
@@ -440,7 +523,13 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
             out_type = 'continue_ar'
             token_val = out_token[0].item()
 
-            box_end_token_id = self.token_ids['box_end_token_id']
+            box_end_token_id = (
+                self.token_ids.get(
+                    'grasp_end_token_id', self.token_ids['box_end_token_id']
+                )
+                if geometry_type == 'contact'
+                else self.token_ids['box_end_token_id']
+            )
             coord_start_token_id = self.token_ids['coord_start_token_id']
             coord_end_token_id = self.token_ids['coord_end_token_id']
             none_token_id = self.token_ids['none_token_id']
@@ -499,7 +588,7 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
             generated = torch.cat([generated, out_token.unsqueeze(0)], dim=1)
 
             # Step 4: Mode switching & termination
-            if out_type == 'im_end':
+            if out_type in ('im_end', 'contact_decode_error'):
                 break
 
             if generation_mode == 'hybrid':
@@ -521,7 +610,11 @@ class LocateAnythingForConditionalGeneration(LocateAnythingPreTrainedModel, Gene
         if verbose:
             end_time = time.time()
             num_tokens = generated_ids.size(1)
-            num_boxes = response[0].count("<box>")
+            num_boxes = (
+                response[0].count("<grasp>")
+                if geometry_type == 'contact'
+                else response[0].count("<box>")
+            )
             total_time = end_time - start_time
 
             out_info =  f"\nStatistic Info, num_tokens={num_tokens}; " + \
diff --git a/Embodied/eaglevl/train/grasp_contact.py b/Embodied/eaglevl/train/grasp_contact.py
new file mode 100644
index 0000000..d758424
--- /dev/null
+++ b/Embodied/eaglevl/train/grasp_contact.py
@@ -0,0 +1,506 @@
+"""Loss utilities for joint two-contact-point PBD training."""
+
+from __future__ import annotations
+
+import math
+from dataclasses import dataclass
+
+import torch
+import torch.nn.functional as F
+
+
+@dataclass
+class ContactLossOutput:
+    pair_sum: torch.Tensor
+    center_sum: torch.Tensor
+    angle_sum: torch.Tensor
+    width_sum: torch.Tensor
+    contact_count: torch.Tensor
+    geometry_active_count: torch.Tensor
+    coord_mass_min: torch.Tensor
+    coord_entropy_max: torch.Tensor
+
+
+def compute_task_adapter_cross_entropy(
+    lm_head: torch.nn.Module,
+    hidden_states: torch.Tensor,
+    labels: torch.Tensor,
+) -> torch.Tensor:
+    """Compute summed CE through the same hooked LM head used by inference."""
+    if hidden_states.ndim != 2 or labels.ndim != 1:
+        raise ValueError(
+            "task-adapter CE expects [tokens, hidden] and [tokens]: "
+            f"hidden={tuple(hidden_states.shape)}, labels={tuple(labels.shape)}"
+        )
+    if hidden_states.shape[0] != labels.shape[0] or labels.numel() == 0:
+        raise ValueError(
+            "task-adapter CE requires one non-empty label per hidden state: "
+            f"hidden={hidden_states.shape[0]}, labels={labels.numel()}"
+        )
+    logits = lm_head(hidden_states)
+    if not torch.isfinite(logits).all():
+        raise FloatingPointError("non-finite logits after grasp output adapter")
+    return F.cross_entropy(logits.float(), labels, reduction="sum")
+
+
+def combine_base_and_pair_ce(
+    base_loss_sum: torch.Tensor,
+    pair_loss_sum: torch.Tensor,
+    global_ce_token_count: torch.Tensor | float | int,
+    pair_weight: float = 1.0,
+) -> torch.Tensor:
+    """Replace four coordinate-token CEs without changing token-level scale.
+
+    ``pair_loss_sum`` contains one four-slot *mean* per contact block.  Convert
+    it back to a token numerator before combining it with the non-coordinate CE
+    numerator.  The denominator must therefore include all valid labels,
+    including the four coordinate slots owned by pair loss.
+    """
+    denominator = torch.as_tensor(
+        global_ce_token_count,
+        device=base_loss_sum.device,
+        dtype=torch.float32,
+    ).clamp_min(1.0)
+    pair_token_sum = 4.0 * pair_loss_sum
+    return (
+        base_loss_sum + float(pair_weight) * pair_token_sum
+    ) / denominator
+
+
+@torch.no_grad()
+def compute_coordinate_token_metrics(
+    lm_head: torch.nn.Module,
+    hidden_states: torch.Tensor,
+    labels: torch.Tensor,
+) -> tuple[torch.Tensor, torch.Tensor, torch.Tensor]:
+    """Measure coordinate-token CE and top-1 accuracy without adding loss."""
+    if hidden_states.ndim != 2 or labels.ndim != 1:
+        raise ValueError(
+            "coordinate metrics expect [tokens, hidden] and [tokens]: "
+            f"hidden={tuple(hidden_states.shape)}, labels={tuple(labels.shape)}"
+        )
+    if hidden_states.shape[0] != labels.shape[0] or labels.numel() == 0:
+        raise ValueError(
+            "coordinate metrics require one non-empty label per hidden state"
+        )
+    logits = lm_head(hidden_states)
+    if not torch.isfinite(logits).all():
+        raise FloatingPointError(
+            "non-finite logits while computing coordinate-token metrics"
+        )
+    logits = logits.float()
+    ce_sum = F.cross_entropy(logits, labels, reduction="sum")
+    correct = logits.argmax(dim=-1).eq(labels).sum()
+    count = labels.new_tensor(labels.numel())
+    return ce_sum.detach(), correct.detach(), count.detach()
+
+
+def configure_llm_lora(model, requested_rank: int) -> bool:
+    """Install LLM LoRA once and keep adapters trainable after base freezing.
+
+    Returns True when a new adapter is installed and False when an adapter from
+    a checkpoint is reused.
+    """
+    requested_rank = int(requested_rank)
+    if requested_rank <= 0:
+        raise ValueError("LLM LoRA rank must be positive")
+
+    configured_rank = int(
+        getattr(model.config, "use_llm_lora", 0) or 0
+    )
+    already_wrapped = bool(getattr(model, "use_llm_lora", False))
+    if already_wrapped:
+        if configured_rank != requested_rank:
+            raise ValueError(
+                "LoRA checkpoint rank does not match requested rank: "
+                f"checkpoint={configured_rank}, requested={requested_rank}"
+            )
+        adapter_parameters = [
+            parameter
+            for name, parameter in model.language_model.named_parameters()
+            if "lora_" in name
+        ]
+        if not adapter_parameters:
+            raise ValueError("model reports LLM LoRA but has no adapter parameters")
+        for parameter in adapter_parameters:
+            parameter.requires_grad = True
+        model.language_model.enable_input_require_grads()
+        return False
+
+    if configured_rank:
+        raise ValueError(
+            "model config requests LLM LoRA but the model is not wrapped: "
+            f"rank={configured_rank}"
+        )
+    model.wrap_llm_lora(
+        r=requested_rank,
+        lora_alpha=2 * requested_rank,
+        lora_dropout=0.05,
+    )
+    model.config.use_llm_lora = requested_rank
+    return True
+
+
+def enable_task_token_training(model, token_ids: list[int]) -> int:
+    """Enable compact grasp-token input/output adapters."""
+    unique_ids = sorted({int(token_id) for token_id in token_ids})
+    if not unique_ids:
+        raise ValueError("task token IDs must not be empty")
+    configured_ids = sorted(
+        int(token_id)
+        for token_id in getattr(model.config, "grasp_task_token_ids", [])
+    )
+    if configured_ids != unique_ids:
+        raise ValueError(
+            "grasp task token IDs differ from the model configuration: "
+            f"configured={configured_ids}, requested={unique_ids}"
+        )
+    input_adapter = getattr(model, "grasp_task_embedding_delta", None)
+    output_adapter = getattr(model, "grasp_task_output_delta", None)
+    if (
+        input_adapter is None
+        or output_adapter is None
+        or input_adapter.shape[0] != len(unique_ids)
+        or output_adapter.shape != input_adapter.shape
+    ):
+        raise ValueError("model has no compatible grasp task embedding adapter")
+    for embedding in (
+        model.language_model.get_input_embeddings(),
+        model.language_model.get_output_embeddings(),
+    ):
+        if embedding.weight.requires_grad:
+            raise ValueError("base embedding/lm_head must remain frozen")
+    input_adapter.requires_grad = True
+    output_adapter.requires_grad = True
+    return model.register_grasp_task_embedding_hook()
+
+
+def initialize_missing_task_adapters(model, missing_keys: list[str]) -> int:
+    """Zero only task adapters absent from the loaded checkpoint.
+
+    Transformers may run its generic missing-weight initializer after the model
+    constructor, so constructor-level ``torch.zeros`` is not sufficient.  A
+    resumed grasp checkpoint must retain its learned adapter values, however.
+    """
+    missing_names = {
+        key.rsplit(".", 1)[-1]
+        for key in missing_keys
+    }
+    initialized_parameters = 0
+    for name in (
+        "grasp_task_embedding_delta",
+        "grasp_task_output_delta",
+    ):
+        parameter = getattr(model, name, None)
+        if parameter is None:
+            continue
+        if name in missing_names:
+            with torch.no_grad():
+                parameter.zero_()
+            initialized_parameters += parameter.numel()
+        if not torch.isfinite(parameter).all():
+            raise FloatingPointError(
+                f"Non-finite values in loaded task adapter: {name}"
+            )
+    return initialized_parameters
+
+
+def validate_coordinate_token_range(start_token_id: int, end_token_id: int) -> None:
+    if end_token_id - start_token_id != 1000:
+        raise ValueError(
+            "contact loss requires exactly 1001 contiguous coordinate tokens; "
+            f"got IDs [{start_token_id}, {end_token_id}]"
+        )
+
+
+def pi_periodic_angle_loss(cosine: torch.Tensor) -> torch.Tensor:
+    """Return a stable, endpoint-exchange-invariant angular loss.
+
+    The forward value is ``1 - |cos(theta)|``.  Expressing the absolute value
+    with an explicit branch gives the ambiguous 90-degree case a deterministic
+    non-zero subgradient.  In contrast, ``1 - cos(theta)^2`` has exactly zero
+    gradient at 90 degrees and cannot recover from that worst-case prediction.
+    """
+    cosine = cosine.clamp(-1.0, 1.0)
+    aligned_cosine = torch.where(cosine >= 0.0, cosine, -cosine)
+    return 1.0 - aligned_cosine
+
+
+def _empty_output(hidden_states: torch.Tensor) -> ContactLossOutput:
+    zero = hidden_states.sum() * 0.0
+    scalar_zero = zero.detach().new_zeros(())
+    return ContactLossOutput(
+        pair_sum=zero,
+        center_sum=zero,
+        angle_sum=zero,
+        width_sum=zero,
+        contact_count=scalar_zero,
+        geometry_active_count=scalar_zero,
+        # Neutral sentinel for a cross-rank MIN reduction when this rank has
+        # no positive contact block.
+        coord_mass_min=scalar_zero.new_ones(()),
+        coord_entropy_max=scalar_zero,
+    )
+
+
+def _filter_colliding_candidates(
+    candidate_mask: torch.Tensor,
+    candidate_collision_2d: torch.Tensor | None,
+    candidate_outside_2d: torch.Tensor | None,
+    collision_valid: torch.Tensor | None,
+    collision_threshold: float,
+    outside_threshold: float,
+) -> torch.Tensor:
+    if candidate_collision_2d is None and candidate_outside_2d is None:
+        return candidate_mask
+
+    safe_candidates = candidate_mask.clone()
+    filterable = torch.zeros(
+        (candidate_mask.shape[0], 1),
+        dtype=torch.bool,
+        device=candidate_mask.device,
+    )
+    if candidate_outside_2d is not None:
+        outside_finite = torch.isfinite(candidate_outside_2d)
+        outside_known = (outside_finite | ~candidate_mask).all(
+            dim=-1, keepdim=True
+        )
+        outside_safe = outside_finite & (
+            candidate_outside_2d <= outside_threshold
+        )
+        safe_candidates &= torch.where(
+            outside_known, outside_safe, candidate_mask
+        )
+        filterable |= outside_known
+
+    if candidate_collision_2d is not None and collision_valid is not None:
+        collision_known = collision_valid.bool().unsqueeze(-1)
+        collision_safe = torch.isfinite(candidate_collision_2d) & (
+            candidate_collision_2d <= collision_threshold
+        )
+        safe_candidates &= torch.where(
+            collision_known, collision_safe, candidate_mask
+        )
+        filterable |= collision_known
+
+    return torch.where(filterable, safe_candidates, candidate_mask)
+
+
+def compute_contact_auxiliary_losses(
+    *,
+    hidden_states: torch.Tensor,
+    lm_head_weight: torch.Tensor,
+    lm_head: torch.nn.Module | None = None,
+    contact_mtp_coord_mask: torch.Tensor,
+    contact_candidates: torch.Tensor,
+    contact_candidate_mask: torch.Tensor,
+    contact_positive_mask: torch.Tensor,
+    contact_image_size: torch.Tensor,
+    coord_start_token_id: int,
+    coord_end_token_id: int,
+    candidate_collision_2d: torch.Tensor | None = None,
+    candidate_outside_2d: torch.Tensor | None = None,
+    collision_valid: torch.Tensor | None = None,
+    collision_threshold: float = 0.0,
+    outside_threshold: float = 0.0,
+    coord_mass_threshold: float = 0.35,
+    coord_entropy_threshold: float = 0.85,
+    minimum_width_diagonal: float = 1e-4,
+) -> ContactLossOutput:
+    """Compute hard-min pair CE and gated image-space geometry losses.
+
+    ``contact_mtp_coord_mask`` is label-aligned. The causal shift is applied here,
+    so each selected hidden state predicts one of the four coordinate labels.
+    """
+
+    validate_coordinate_token_range(coord_start_token_id, coord_end_token_id)
+    if contact_mtp_coord_mask.shape != hidden_states.shape[:2]:
+        raise ValueError(
+            "contact coordinate mask must match hidden sequence shape: "
+            f"mask={tuple(contact_mtp_coord_mask.shape)}, "
+            f"hidden={tuple(hidden_states.shape[:2])}"
+        )
+
+    shift_mask = contact_mtp_coord_mask[..., 1:].bool().reshape(-1)
+    selected_hidden = hidden_states[..., :-1, :].reshape(
+        -1, hidden_states.shape[-1]
+    )[shift_mask]
+    if selected_hidden.numel() == 0:
+        if contact_positive_mask.bool().any():
+            raise ValueError("positive contact sample has no four-coordinate MTP block")
+        return _empty_output(hidden_states)
+    if selected_hidden.shape[0] % 4 != 0:
+        raise ValueError(
+            "contact MTP coordinate mask must select a multiple of four slots; "
+            f"got {selected_hidden.shape[0]}"
+        )
+
+    block_count = selected_hidden.shape[0] // 4
+    positive_indices = contact_positive_mask.bool().nonzero(as_tuple=False).flatten()
+    if positive_indices.numel() != block_count:
+        raise ValueError(
+            "positive sample/contact block count mismatch: "
+            f"positive_samples={positive_indices.numel()}, blocks={block_count}"
+        )
+
+    candidates = contact_candidates[positive_indices].long()
+    candidate_mask = contact_candidate_mask[positive_indices].bool()
+    image_size = contact_image_size[positive_indices].float()
+    if candidates.ndim != 3 or candidates.shape[-1] != 4:
+        raise ValueError(
+            "contact_candidates must have shape [samples, K, 4], "
+            f"got {candidates.shape}"
+        )
+    if not candidate_mask.any(dim=-1).all():
+        raise ValueError("every positive contact sample needs at least one candidate")
+    if (candidates[candidate_mask] < 0).any() or (
+        candidates[candidate_mask] > 1000
+    ).any():
+        raise ValueError("contact candidate values must be in [0, 1000]")
+    if (image_size <= 0).any() or not torch.isfinite(image_size).all():
+        raise ValueError("positive contact samples need finite positive image sizes")
+
+    if candidate_collision_2d is not None:
+        candidate_collision_2d = candidate_collision_2d[positive_indices].float()
+    if candidate_outside_2d is not None:
+        candidate_outside_2d = candidate_outside_2d[positive_indices].float()
+    if collision_valid is not None:
+        collision_valid = collision_valid[positive_indices].bool()
+    effective_mask = _filter_colliding_candidates(
+        candidate_mask,
+        candidate_collision_2d,
+        candidate_outside_2d,
+        collision_valid,
+        collision_threshold,
+        outside_threshold,
+    )
+
+    valid_block_mask = effective_mask.any(dim=-1)
+    if not valid_block_mask.any():
+        return _empty_output(hidden_states)
+    if not valid_block_mask.all():
+        selected_hidden = selected_hidden.reshape(block_count, 4, -1)[
+            valid_block_mask
+        ].reshape(-1, hidden_states.shape[-1])
+        candidates = candidates[valid_block_mask]
+        effective_mask = effective_mask[valid_block_mask]
+        image_size = image_size[valid_block_mask]
+        block_count = int(valid_block_mask.sum().item())
+
+    block_hidden = selected_hidden.reshape(block_count, 4, -1)
+    if lm_head is None:
+        logits = torch.einsum(
+            "bph,vh->bpv",
+            block_hidden.to(dtype=lm_head_weight.dtype),
+            lm_head_weight,
+        )
+    else:
+        # Calling the module, rather than F.linear on its weight, applies the
+        # exact grasp-token output hook used by generation at every slot.
+        logits = lm_head(block_hidden.to(dtype=lm_head_weight.dtype))
+    logits = logits.float()
+    log_probs = F.log_softmax(logits, dim=-1)
+
+    target_ids = candidates + coord_start_token_id
+    expanded_log_probs = log_probs.unsqueeze(1).expand(
+        -1, candidates.shape[1], -1, -1
+    )
+    identity_log_probs = expanded_log_probs.gather(
+        -1, target_ids.unsqueeze(-1)
+    ).squeeze(-1)
+    swapped_ids = target_ids[..., [2, 3, 0, 1]]
+    swapped_log_probs = expanded_log_probs.gather(
+        -1, swapped_ids.unsqueeze(-1)
+    ).squeeze(-1)
+    identity_loss = -identity_log_probs.mean(dim=-1)
+    swapped_loss = -swapped_log_probs.mean(dim=-1)
+    candidate_orientation_loss = torch.stack(
+        (identity_loss, swapped_loss), dim=-1
+    )
+    candidate_orientation_loss = candidate_orientation_loss.masked_fill(
+        ~effective_mask.unsqueeze(-1), torch.inf
+    )
+    pair_per_block, flat_best_index = candidate_orientation_loss.flatten(1).min(
+        dim=-1
+    )
+    if not torch.isfinite(pair_per_block).all():
+        raise FloatingPointError("non-finite contact pair loss")
+    best_candidate_index = torch.div(
+        flat_best_index, 2, rounding_mode="floor"
+    )
+    batch_index = torch.arange(block_count, device=hidden_states.device)
+    best_target = candidates[batch_index, best_candidate_index].float()
+
+    coord_probs = log_probs[
+        ..., coord_start_token_id : coord_end_token_id + 1
+    ].exp()
+    coord_mass = coord_probs.sum(dim=-1)
+    conditional_coord_probs = coord_probs / coord_mass.unsqueeze(-1).clamp_min(1e-12)
+    coordinate_values = torch.arange(
+        1001, device=hidden_states.device, dtype=torch.float32
+    )
+    soft_coordinates = (
+        conditional_coord_probs * coordinate_values.view(1, 1, -1)
+    ).sum(dim=-1)
+    coord_entropy = -(
+        conditional_coord_probs
+        * conditional_coord_probs.clamp_min(1e-12).log()
+    ).sum(dim=-1) / math.log(1001.0)
+
+    widths_heights = image_size[:, [0, 1, 0, 1]]
+    predicted_pixels = soft_coordinates * widths_heights / 1000.0
+    target_pixels = best_target * widths_heights / 1000.0
+    image_diagonal = torch.linalg.vector_norm(image_size, dim=-1).clamp_min(1e-6)
+
+    predicted_center = 0.5 * (
+        predicted_pixels[:, :2] + predicted_pixels[:, 2:]
+    )
+    target_center = 0.5 * (target_pixels[:, :2] + target_pixels[:, 2:])
+    # Apply SmoothL1 in pixel space, then normalize the resulting loss.  Doing
+    # the normalization first puts ordinary errors inside SmoothL1's quadratic
+    # region and shrinks useful geometry gradients by another diagonal factor.
+    center_per_block = F.smooth_l1_loss(
+        predicted_center,
+        target_center,
+        reduction="none",
+    ).mean(dim=-1) / image_diagonal
+
+    predicted_vector = predicted_pixels[:, 2:] - predicted_pixels[:, :2]
+    target_vector = target_pixels[:, 2:] - target_pixels[:, :2]
+    predicted_width = torch.linalg.vector_norm(predicted_vector, dim=-1)
+    target_width = torch.linalg.vector_norm(target_vector, dim=-1)
+    width_per_block = F.smooth_l1_loss(
+        predicted_width,
+        target_width,
+        reduction="none",
+    ) / image_diagonal
+    cosine = (predicted_vector * target_vector).sum(dim=-1) / (
+        predicted_width.clamp_min(1e-6) * target_width.clamp_min(1e-6)
+    )
+    angle_per_block = pi_periodic_angle_loss(cosine)
+
+    confidence_gate = (
+        (coord_mass.min(dim=-1).values >= coord_mass_threshold)
+        & (coord_entropy.max(dim=-1).values <= coord_entropy_threshold)
+    ).detach()
+    valid_angle = (
+        confidence_gate
+        & (predicted_width / image_diagonal >= minimum_width_diagonal)
+        & (target_width / image_diagonal >= minimum_width_diagonal)
+    ).detach()
+    confidence = confidence_gate.float()
+    angle_gate = valid_angle.float()
+    output = ContactLossOutput(
+        pair_sum=pair_per_block.sum(),
+        center_sum=(center_per_block * confidence).sum(),
+        angle_sum=(angle_per_block * angle_gate).sum(),
+        width_sum=(width_per_block * confidence).sum(),
+        contact_count=confidence.new_tensor(float(block_count)),
+        geometry_active_count=confidence.sum().detach(),
+        coord_mass_min=coord_mass.min().detach(),
+        coord_entropy_max=coord_entropy.max().detach(),
+    )
+    for name in ("pair_sum", "center_sum", "angle_sum", "width_sum"):
+        if not torch.isfinite(getattr(output, name)):
+            raise FloatingPointError(f"non-finite contact loss component: {name}")
+    return output
diff --git a/Embodied/eaglevl/utils/locany/grasp_adapter_utils.py b/Embodied/eaglevl/utils/locany/grasp_adapter_utils.py
new file mode 100644
index 0000000..cb6c74e
--- /dev/null
+++ b/Embodied/eaglevl/utils/locany/grasp_adapter_utils.py
@@ -0,0 +1,35 @@
+"""Shared grasp task-token adapter operations for training and inference."""
+
+from __future__ import annotations
+
+import torch
+import torch.nn.functional as F
+
+
+def apply_grasp_task_output_delta(
+    hidden_states: torch.Tensor,
+    logits: torch.Tensor,
+    task_token_ids: torch.Tensor,
+    output_delta: torch.Tensor,
+) -> torch.Tensor:
+    """Apply the compact grasp-token output adapter to every logit row."""
+    if hidden_states.shape[:-1] != logits.shape[:-1]:
+        raise ValueError(
+            "hidden-state/logit leading dimensions differ: "
+            f"hidden={tuple(hidden_states.shape)}, logits={tuple(logits.shape)}"
+        )
+    if output_delta.ndim != 2 or output_delta.shape[1] != hidden_states.shape[-1]:
+        raise ValueError(
+            "grasp output adapter has incompatible shape: "
+            f"delta={tuple(output_delta.shape)}, hidden={tuple(hidden_states.shape)}"
+        )
+    task_token_ids = task_token_ids.to(device=logits.device, dtype=torch.long)
+    if task_token_ids.numel() != output_delta.shape[0]:
+        raise ValueError(
+            "grasp task-token IDs and output-adapter rows differ: "
+            f"tokens={task_token_ids.numel()}, rows={output_delta.shape[0]}"
+        )
+    correction = F.linear(
+        hidden_states.to(dtype=output_delta.dtype), output_delta
+    ).to(dtype=logits.dtype)
+    return logits.index_add(-1, task_token_ids, correction)
